==== src/VerifySample.Tests/ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/new?step=output&os=Windows&ide=Rider&cli=Cli&tf=XunitV3&ci=GitHubActions&ext=Playwright&min=Playwright&sponsor=Exempt&exempt=OpenSource&until=2027-09
// Delete whatever is not needed.
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.Playwright: an IPage, IElementHandle or ILocator is verified as its html and as a
        // screenshot png, so both the markup and what the page looks like are covered.
        // installPlaywright: true runs the Playwright browser install once when the assembly loads,
        // so a clean machine or build agent downloads the browsers instead of failing.
        VerifyPlaywright.Initialize(installPlaywright: true);

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}

==== src/VerifySample.Tests/Extensions/PlaywrightTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/new?step=output&os=Windows&ide=Rider&cli=Cli&tf=XunitV3&ci=GitHubActions&ext=Playwright&min=Playwright&sponsor=Exempt&exempt=OpenSource&until=2027-09
// Delete whatever is not needed.
using Microsoft.Playwright;
using VerifyTestsPlaywright;

// Verify.Playwright: Verifies a Playwright page, element or locator as both its html and a screenshot png.
// https://github.com/VerifyTests/Verify.HeadlessBrowsers
// Note: Each verification writes two files, the html and a png screenshot, unless screenshotOnly:
// true is passed.
// Note: Screenshots vary between operating systems and renderers, so an image comparer, or
// VerifierSettings.UseSsimForPng(...), is what keeps CI stable.
// Note: --disable-lcd-text makes text rendering reproducible; existing screenshots have to be
// re-accepted once after adding it.
// Note: Setting Path on any screenshot options throws "ScreenshotOptions Path not supported.".
// Note: SocketWaiter is defined separately by each of the three headless browser packages, here in
// VerifyTestsPlaywright, so the usings are per file.
// Note: Verify.Playwright, Verify.Puppeteer and Verify.Selenium ship from one repository and share
// a version.
// Note: The package targets net10.0 only.
public class PlaywrightTests
{
    // Two files are written: the page html and a png screenshot.
    // --disable-lcd-text turns off subpixel text rendering, which otherwise differs between
    // machines and makes the screenshot fail on a build agent.
    // Not run by default: needs Chromium installed and the site under test listening on
    // http://localhost:5000
    [Fact(Skip = "needs Chromium installed and the site under test listening on http://localhost:5000")]
    public async Task Page()
    {
        // Waits for the site under test to start before driving the browser.
        await SocketWaiter.Wait(port: 5000);

        using var playwright = await Playwright.CreateAsync();
        await using var browser = await playwright.Chromium.LaunchAsync(
            new()
            {
                Args = ["--disable-lcd-text"]
            });
        var page = await browser.NewPageAsync();
        await page.GotoAsync("http://localhost:5000");

        await Verify(page);
    }
}


==== packages

Verify.XunitV3
xunit.v3
Verify.Playwright
Microsoft.Playwright

==== interactions

Warning external-tool: Playwright needs A Playwright browser: Playwright drives a real Chromium, which has to be downloaded once. Initialize(installPlaywright: true) does it on assembly load, and the samples also need the site under test running.