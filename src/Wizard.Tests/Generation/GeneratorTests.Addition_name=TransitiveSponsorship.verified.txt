==== interactions

Warning transitive-sponsorship [OpenXml]

==== readme.md

# Adding Verify extensions

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml) (reopen that link to change any answer).

 * Test framework: xUnit v3
 * Adding: OpenXml (verbose)
 * Already in the project: none listed
 * Maintenance fee: No change
 * Package versions: the newest stable releases as of 2026-09-22; nuget.org was not checked when this was generated

The download holds the changes below as files to merge, each named for where it goes. `CLAUDE.md` has the same steps written for an AI assistant to carry out.

## 1. Add the packages

With Central Package Management, merge `Directory.Packages.props.fragment.xml` into `Directory.Packages.props`, and the `PackageReference` items in `TestProject.csproj.fragment.xml` into the test project:

```xml
<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.OpenXml" Version="1.0.0" />
  <PackageVersion Include="DocumentFormat.OpenXml" Version="1.0.0" />
</ItemGroup>
```

Without it, reference each package with its version instead:

```xml
<PackageReference Include="Verify.OpenXml" Version="1.0.0" />
<PackageReference Include="DocumentFormat.OpenXml" Version="1.0.0" />
```

Or from the command line, in the test project's directory:

```
dotnet add package Verify.OpenXml
dotnet add package DocumentFormat.OpenXml
```

## 2. Update the test project

`TestProject.csproj.fragment.xml` has everything the test project gains:

```xml
<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.OpenXml" />
  <PackageReference Include="DocumentFormat.OpenXml" />
</ItemGroup>
```

## 3. Update the module initializer

`ModuleInitializer.cs` is a complete initializer for the extensions being added. To merge it into the project's own: explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, because a plugin can only be initialized once and a call with parameters has to get in before discovery does. Calls after `InitializePlugins()` stay after it.

```cs
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.OpenXml: the paper size used when a document states none. Left null it follows the
        // machine's region, so a rendered page would differ between a US and a European agent.
        // False is A4, true is US Letter. It only matters once a render backend is referenced.
        VerifyOpenXml.UseLetterPageSize = false;
        // VerifyOpenXml.FontDirectory = Path.Combine(AppContext.BaseDirectory, "Fonts");

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}
```

## 4. Add the tests

Copy `Extensions/` into the test project. Each file shows one extension, with a comment above every test saying what it demonstrates.

The tests exercise sample types in `src-samples/`: a DbContext, a controller, a message handler. They stand in for the project's own types. Either copy them into a project the tests reference, or point the tests at the real ones.

## Extensions

Each extension below teaches Verify about one kind of value: how to serialize it, how to compare two of them, or how to turn it into something readable. They are independent; removing one changes nothing else.

### Verify.OpenXml

Verifies Word, Excel and PowerPoint files: text, a csv per sheet, properties, a deterministic copy and page renders. ([repository](https://github.com/VerifyTests/Verify.OpenXml))

Packages: `Verify.OpenXml` 1.0.0, `DocumentFormat.OpenXml` 1.0.0 (builds the sample documents, so no fixture file has to be added).

Enabled by:

```cs
VerifyOpenXml.UseLetterPageSize = false;
```

`Extensions/OpenXmlTests.cs` in the download shows every documented API:

**Word** — The extension passed to Verify decides which converter runs. A docx writes an info file with the document properties, the extracted text, and a deterministic copy of the file.

```cs
return Verify(WordDocument.Build(), "docx");
```

**Excel** — A workbook writes one .verified.csv per sheet, plus an info file listing the sheets and their columns, so a changed cell shows up as a one line diff.

```cs
return Verify(ExcelDocument.Build(), "xlsx");
```

**WordDocumentObject** — An open SpreadsheetDocument, WordprocessingDocument or PresentationDocument can be verified directly, which is useful when the test already has one in hand.

```cs
await using var stream = WordDocument.Build();
using var document = WordprocessingDocument.Open(stream, false);
await Verify(document);
```

**ExcludeExcelPackage** — Building the deterministic package is expensive, and committing a binary is not always wanted. This drops it and skips building it; the info, csv and rendered pages still verify.

```cs
return Verify(ExcelDocument.Build(), "xlsx")
    .ExcludeTargets("xlsx");
```

**UniqueForRuntime** — The xml inside the package is identical across runtimes, but the Deflate implementation differs, so the binary target needs a snapshot per runtime when the project multi targets.

```cs
return Verify(WordDocument.Build(), "docx")
    .UniqueForRuntime();
```

Notes:

 * One verification writes several targets: an info txt, the text or a csv per sheet, the deterministic binary, and a png per page once a backend is referenced.
 * Png rendering needs exactly one of `Morph.Skia` or `Morph.ImageSharp`. With neither, rendering is silently skipped; with both, the first verification throws.
 * The backend is found by probing for the assembly, so switching it is a package change only; no code changes.
 * Rendering is compiled in on net10.0 only, because Morph targets net10.0.
 * Rendered pages depend on the installed fonts, so set `VerifyOpenXml.FontDirectory` to a bundled font folder and generate the `.verified.png` files on one canonical machine.
 * Page counts differ per format: Word is one page per laid out page, PowerPoint one per slide in `p:sldIdLst` order, and Excel follows the print layout rather than the sheet.
 * pptx is verified the same way, by passing a stream with the extension `pptx` or a `PresentationDocument`.
 * `UniqueForOSPlatform()` is worth adding once rendering is on and the tests run on more than one OS.

## Interactions between the selected extensions

### Warning: OpenXml

Morph carries an Open Source Maintenance Fee check of its own, for Papyrine, and a sponsorship is made to one project, so Papyrine needs its own. The generated Directory.Build.props has that owner's options commented out; until one is chosen the build fails with SC021.

## Open Source Maintenance Fee

Merge `Directory.Build.props.fragment.xml` into `Directory.Build.props`:

```xml
<!-- Merge into Directory.Build.props. -->
<!-- Morph carries the same check under its own prefix, for Papyrine.
     The project's existing declaration uses the Verify_ prefix and does not cover this
     owner. The build fails with SC021 until one of these is chosen.
     Uncomment and complete exactly one: -->
<!-- <PropertyGroup>
       <Papyrine_GitHubSponsorAccount>your-github-account</Papyrine_GitHubSponsorAccount>
     </PropertyGroup>

     An individual, or an organization with annual gross revenue under US$10,000 (government agencies excluded):
     <PropertyGroup>
       <Papyrine_SponsorshipExemption>SmallRevenue</Papyrine_SponsorshipExemption>
       <Papyrine_SponsorshipExemptionUntil>yyyy-MM</Papyrine_SponsorshipExemptionUntil>
     </PropertyGroup>

     An organization that engaged the core maintainers for consulting work, for six months from the end of that engagement:
     <PropertyGroup>
       <Papyrine_SponsorshipExemption>MaintainerConsulting</Papyrine_SponsorshipExemption>
       <Papyrine_SponsorshipExemptionUntil>yyyy-MM</Papyrine_SponsorshipExemptionUntil>
     </PropertyGroup>

     Or opt out, which logs a breach-of-license warning on every build:
     <PropertyGroup>
       <Papyrine_SponsorshipLicenseIgnored>true</Papyrine_SponsorshipLicenseIgnored>
     </PropertyGroup> -->
```

## Running the tests

A new test has no `.verified.` file, so its first run fails and writes a `.received.` file. Review it, then accept it by renaming it to `.verified.`, accepting it in the diff tool, DiffEngineTray or IDE plugin, or running `dotnet verify accept`.

Set the environment variable `DiffEngine_Disabled=true` when running tests where a diff tool should not open, such as a build server or an AI assistant's terminal. Verify detects the common build servers itself.

## Further reading

 * [Verify readme](https://github.com/VerifyTests/Verify#readme)
 * [Serializer settings](https://github.com/VerifyTests/Verify/blob/main/docs/serializer-settings.md)
 * [Scrubbers](https://github.com/VerifyTests/Verify/blob/main/docs/scrubbers.md)
 * [File naming](https://github.com/VerifyTests/Verify/blob/main/docs/naming.md)
 * [Parameterised tests](https://github.com/VerifyTests/Verify/blob/main/docs/parameterised.md)
 * [Plugins](https://github.com/VerifyTests/Verify/blob/main/docs/plugins.md)
 * [Using Verify with AI assistants](https://github.com/VerifyTests/Verify/blob/main/docs/ai-usage.md)

==== CLAUDE.md

# Adding Verify extensions: instructions for AI coding assistants

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml). The files next to this one are changes to merge into a project that already uses Verify snapshot testing with xUnit v3. Carry out the steps below in order.

## Steps

1. If the solution uses Central Package Management (a `Directory.Packages.props` with `ManagePackageVersionsCentrally`), merge the `PackageVersion` items from `Directory.Packages.props.fragment.xml` into it. Otherwise give each `PackageReference` in the next step the version listed in that file.
2. Merge `TestProject.csproj.fragment.xml` into the test project: the `PackageReference` items, and any properties and items it has.
3. Merge `ModuleInitializer.cs` into the test project's existing module initializer. Explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, in the order given: a plugin can only be initialized once, so a call with parameters has to come before discovery. Calls after `InitializePlugins()` stay after it. If the project has no module initializer, add the file as it is.
4. Copy `Extensions/` into the test project.
5. The tests use placeholder types from `src-samples/`. Prefer pointing the tests at the project's real types; otherwise copy the placeholders into a project the tests reference.
6. Merge `Directory.Build.props.fragment.xml` into `Directory.Build.props`. A block that is commented out is a decision for the user; do not choose one of its options on their behalf.
7. Build, then run the tests with `DiffEngine_Disabled=true`. The new tests fail on their first run because they have no `.verified.` files yet. Show the user each `.received.` file; accept one only once the user agrees it is right.

## Verify extensions in this project

### Verify.OpenXml

 * Verifies Word, Excel and PowerPoint files: text, a csv per sheet, properties, a deterministic copy and page renders.
 * Repository: https://github.com/VerifyTests/Verify.OpenXml
 * Enabled in `ModuleInitializer.cs` by `VerifyOpenXml.UseLetterPageSize = false;`. Do not move that call after `VerifierSettings.InitializePlugins()`.
 * Samples: `Extensions/OpenXmlTests.cs`, using `Word`, `Excel`, `WordDocumentObject`, `ExcludeExcelPackage`, `UniqueForRuntime`.
 * Needs `using DocumentFormat.OpenXml.Packaging;` in the test file, not in global usings: extensions define colliding type names.
 * One verification writes several targets: an info txt, the text or a csv per sheet, the deterministic binary, and a png per page once a backend is referenced.
 * Png rendering needs exactly one of `Morph.Skia` or `Morph.ImageSharp`. With neither, rendering is silently skipped; with both, the first verification throws.
 * The backend is found by probing for the assembly, so switching it is a package change only; no code changes.
 * Rendering is compiled in on net10.0 only, because Morph targets net10.0.
 * Rendered pages depend on the installed fonts, so set `VerifyOpenXml.FontDirectory` to a bundled font folder and generate the `.verified.png` files on one canonical machine.
 * Page counts differ per format: Word is one page per laid out page, PowerPoint one per slide in `p:sldIdLst` order, and Excel follows the print layout rather than the sheet.
 * pptx is verified the same way, by passing a stream with the extension `pptx` or a `PresentationDocument`.
 * `UniqueForOSPlatform()` is worth adding once rendering is on and the tests run on more than one OS.

## Decisions already taken

These follow from the combination of extensions. Changing one of them changes behaviour across the whole test project.

 * **OpenXml**: Morph carries an Open Source Maintenance Fee check of its own, for Papyrine, and a sponsorship is made to one project, so Papyrine needs its own. The generated Directory.Build.props has that owner's options commented out; until one is chosen the build fails with SC021.

## Snapshot Testing (Verify)

This project uses [Verify](https://github.com/VerifyTests/Verify) for snapshot testing.

### Key concepts

- **`.verified.*` files** are the approved snapshots. They are committed to source control.
- **`.received.*` files** are the actual output from the latest test run. They are generated when a test fails (actual != expected) and are git-ignored.
- When a test fails, compare the `.received.*` file to the `.verified.*` file to understand the difference.

### Handling test failures

When a snapshot test fails:

1. Run the failing test and read the `.received.*` file that was generated.
2. Compare it to the corresponding `.verified.*` file.
3. Determine if the difference is expected (due to an intentional code change) or a bug.
   - **If expected**: copy the `.received.*` file to the `Verified:` path listed in the exception message to accept the new snapshot.
   - **If a bug**: fix the code, not the snapshot.
4. Never hand-edit `.verified.*` files to make tests pass. Always let Verify generate the correct output by running the test.
5. Never derive the verified name from the received name. The two are not always identical. Multi-targeted projects add a runtime and version suffix to the received file, and ignored parameters are kept in the received name but dropped from the verified name. Always take the destination from the `Verified:` line of the exception message.
6. Delete every `.verified.` file listed in the `Delete:` section. Those files are no longer produced by any test, usually because a test was renamed or removed.

If a snapshot differs between runs or between machines, add a scrubber to the test (`ScrubLinesContaining(...)`, `ScrubLines(...)`, `AddScrubber(...)`) rather than accepting the new output each time.

### Scrubbed values

Verify replaces non-deterministic values in snapshots with stable placeholders:

- GUIDs become `Guid_1`, `Guid_2`, etc.
- DateTimes become `DateTime_1`, `DateTime_2`, etc.
- File paths are replaced with tokens like `{SolutionDirectory}`, `{ProjectDirectory}`, `{TempPath}`.
- Custom scrubbers may replace other values.

These placeholders are intentional and ensure snapshots are deterministic across machines and runs.

### Exception message format

When a test fails, the exception message is machine-parsable with the following structure:

```
Directory: /path/to/test/project
New:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt
NotEqual:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt

FileContent:

New:

Received: TestClass.Method.received.txt
<file content here>

NotEqual:

Received: TestClass.Method.received.txt
<received content>
Verified: TestClass.Method.verified.txt
<verified content>
```

 * The `Directory:` line gives the base path for all relative file references.
 * `New` means no `.verified.` file exists yet (first run or new test).
 * `NotEqual` means the `.received.` and `.verified.` files differ.
 * `Delete` means a `.verified.` file is no longer produced by any test.
 * The `FileContent:` section contains the actual text content for quick comparison without needing to read files separately.

See [Exception Message Format](https://github.com/VerifyTests/Verify/blob/main/docs/exception-message-format.md) for full details.

### Verified file conventions

- Encoding: UTF-8 with BOM
- Line endings: LF (not CRLF)
- No trailing newline

### Common patterns

```csharp
// Verify an object (serialized to JSON)
await Verify(myObject);

// Verify a string
await Verify("some text output");

// Verify with inline settings
await Verify(myObject)
    .ScrubLines(line => line.Contains("volatile"))
    .DontScrubDateTimes();

// Verify throws
await ThrowsTask(() => MethodThatThrows())
    .IgnoreStackTrace();
```

==== Directory.Packages.props.fragment.xml

<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.OpenXml" Version="1.0.0" />
  <PackageVersion Include="DocumentFormat.OpenXml" Version="1.0.0" />
</ItemGroup>

==== TestProject.csproj.fragment.xml

<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.OpenXml" />
  <PackageReference Include="DocumentFormat.OpenXml" />
</ItemGroup>

==== ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml
// Delete whatever is not needed.
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.OpenXml: the paper size used when a document states none. Left null it follows the
        // machine's region, so a rendered page would differ between a US and a European agent.
        // False is A4, true is US Letter. It only matters once a render backend is referenced.
        VerifyOpenXml.UseLetterPageSize = false;
        // VerifyOpenXml.FontDirectory = Path.Combine(AppContext.BaseDirectory, "Fonts");

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}

==== Directory.Build.props.fragment.xml

<!-- Merge into Directory.Build.props. -->
<!-- Morph carries the same check under its own prefix, for Papyrine.
     The project's existing declaration uses the Verify_ prefix and does not cover this
     owner. The build fails with SC021 until one of these is chosen.
     Uncomment and complete exactly one: -->
<!-- <PropertyGroup>
       <Papyrine_GitHubSponsorAccount>your-github-account</Papyrine_GitHubSponsorAccount>
     </PropertyGroup>

     An individual, or an organization with annual gross revenue under US$10,000 (government agencies excluded):
     <PropertyGroup>
       <Papyrine_SponsorshipExemption>SmallRevenue</Papyrine_SponsorshipExemption>
       <Papyrine_SponsorshipExemptionUntil>yyyy-MM</Papyrine_SponsorshipExemptionUntil>
     </PropertyGroup>

     An organization that engaged the core maintainers for consulting work, for six months from the end of that engagement:
     <PropertyGroup>
       <Papyrine_SponsorshipExemption>MaintainerConsulting</Papyrine_SponsorshipExemption>
       <Papyrine_SponsorshipExemptionUntil>yyyy-MM</Papyrine_SponsorshipExemptionUntil>
     </PropertyGroup>

     Or opt out, which logs a breach-of-license warning on every build:
     <PropertyGroup>
       <Papyrine_SponsorshipLicenseIgnored>true</Papyrine_SponsorshipLicenseIgnored>
     </PropertyGroup> -->

==== Extensions/OpenXmlTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml
// Delete whatever is not needed.
using DocumentFormat.OpenXml.Packaging;

// Verify.OpenXml: Verifies Word, Excel and PowerPoint files: text, a csv per sheet, properties, a deterministic copy and page renders.
// https://github.com/VerifyTests/Verify.OpenXml
// Note: One verification writes several targets: an info txt, the text or a csv per sheet, the
// deterministic binary, and a png per page once a backend is referenced.
// Note: Png rendering needs exactly one of Morph.Skia or Morph.ImageSharp. With neither, rendering
// is silently skipped; with both, the first verification throws.
// Note: The backend is found by probing for the assembly, so switching it is a package change
// only; no code changes.
// Note: Rendering is compiled in on net10.0 only, because Morph targets net10.0.
// Note: Rendered pages depend on the installed fonts, so set VerifyOpenXml.FontDirectory to a
// bundled font folder and generate the .verified.png files on one canonical machine.
// Note: Page counts differ per format: Word is one page per laid out page, PowerPoint one per
// slide in p:sldIdLst order, and Excel follows the print layout rather than the sheet.
// Note: pptx is verified the same way, by passing a stream with the extension pptx or a
// PresentationDocument.
// Note: UniqueForOSPlatform() is worth adding once rendering is on and the tests run on more than
// one OS.
public class OpenXmlTests
{
    // The extension passed to Verify decides which converter runs. A docx writes an info file
    // with the document properties, the extracted text, and a deterministic copy of the file.
    [Fact]
    public Task Word()
    {
        return Verify(WordDocument.Build(), "docx");
    }

    // A workbook writes one .verified.csv per sheet, plus an info file listing the sheets and
    // their columns, so a changed cell shows up as a one line diff.
    [Fact]
    public Task Excel()
    {
        return Verify(ExcelDocument.Build(), "xlsx");
    }

    // An open SpreadsheetDocument, WordprocessingDocument or PresentationDocument can be
    // verified directly, which is useful when the test already has one in hand.
    [Fact]
    public async Task WordDocumentObject()
    {
        await using var stream = WordDocument.Build();
        using var document = WordprocessingDocument.Open(stream, false);
        await Verify(document);
    }

    // Building the deterministic package is expensive, and committing a binary is not always
    // wanted. This drops it and skips building it; the info, csv and rendered pages still verify.
    [Fact]
    public Task ExcludeExcelPackage()
    {
        return Verify(ExcelDocument.Build(), "xlsx")
            .ExcludeTargets("xlsx");
    }

    // The xml inside the package is identical across runtimes, but the Deflate implementation
    // differs, so the binary target needs a snapshot per runtime when the project multi targets.
    [Fact]
    public Task UniqueForRuntime()
    {
        return Verify(WordDocument.Build(), "docx")
            .UniqueForRuntime();
    }
}

==== src-samples/WordDocument.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml
// Delete whatever is not needed.
using DocumentFormat.OpenXml;
using DocumentFormat.OpenXml.Packaging;
using DocumentFormat.OpenXml.Wordprocessing;

// Stands in for the reader's own document. It is built in code so the samples need no
// fixture file, and it lives here because the Wordprocessing and Spreadsheet namespaces
// define colliding type names and cannot be imported into one file.
public static class WordDocument
{
    public static MemoryStream Build()
    {
        var stream = new MemoryStream();
        using (var document = WordprocessingDocument.Create(stream, WordprocessingDocumentType.Document))
        {
            var part = document.AddMainDocumentPart();
            part.Document = new(
                new Body(
                    new Paragraph(
                        new Run(
                            new Text("Hello World! This is a sample Word document."))),
                    new Paragraph(
                        new Run(
                            new Text("This is the second paragraph with some more text.")))));
        }

        return new(stream.ToArray());
    }
}

==== src-samples/ExcelDocument.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=OpenXml
// Delete whatever is not needed.
using DocumentFormat.OpenXml;
using DocumentFormat.OpenXml.Packaging;
using DocumentFormat.OpenXml.Spreadsheet;

// Stands in for the reader's own workbook, built in code for the same reason as WordDocument.
public static class ExcelDocument
{
    public static MemoryStream Build()
    {
        var stream = new MemoryStream();
        using (var document = SpreadsheetDocument.Create(stream, SpreadsheetDocumentType.Workbook))
        {
            var workbookPart = document.AddWorkbookPart();
            workbookPart.Workbook = new();
            var worksheetPart = workbookPart.AddNewPart<WorksheetPart>();
            worksheetPart.Worksheet = new(
                new SheetData(
                    BuildRow("First Name", "Last Name", "Country"),
                    BuildRow("Dulce", "Abril", "United States"),
                    BuildRow("Mara", "Hashimoto", "Great Britain")));
            workbookPart.Workbook
                .AppendChild(new Sheets())
                .AppendChild(
                    new Sheet
                    {
                        Id = workbookPart.GetIdOfPart(worksheetPart),
                        SheetId = 1U,
                        Name = "Sheet1"
                    });
        }

        return new(stream.ToArray());
    }

    static Row BuildRow(params string[] values)
    {
        var row = new Row();
        foreach (var value in values)
        {
            row.AppendChild(
                new Cell
                {
                    DataType = CellValues.String,
                    CellValue = new(value)
                });
        }

        return row;
    }
}

