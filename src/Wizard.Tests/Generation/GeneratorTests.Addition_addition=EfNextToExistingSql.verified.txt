==== interactions

Warning ef-sql-recording [EntityFramework, SqlServer]
Info recording-bus [EntityFramework, SqlServer]

==== readme.md

# Adding Verify extensions

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=DiffPlex,SqlServer&ext=EntityFramework) (reopen that link to change any answer).

 * Test framework: xUnit v3
 * Adding: EntityFramework (verbose)
 * Already in the project: DiffPlex, SqlServer
 * Maintenance fee: No change

The download holds the changes below as files to merge, each named for where it goes. `CLAUDE.md` has the same steps written for an AI assistant to carry out.

## 1. Add the packages

With Central Package Management, merge `Directory.Packages.props.fragment.xml` into `Directory.Packages.props`, and the `PackageReference` items in `TestProject.csproj.fragment.xml` into the test project:

```xml
<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.EntityFramework" Version="15.5.0" />
  <PackageVersion Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.12" />
</ItemGroup>
```

Without it, reference each package with its version instead:

```xml
<PackageReference Include="Verify.EntityFramework" Version="15.5.0" />
<PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.12" />
```

Or from the command line, in the test project's directory:

```
dotnet add package Verify.EntityFramework
dotnet add package Microsoft.EntityFrameworkCore.SqlServer
```

## 2. Update the test project

`TestProject.csproj.fragment.xml` has everything the test project gains:

```xml
<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.EntityFramework" />
  <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
</ItemGroup>
```

## 3. Update the module initializer

`ModuleInitializer.cs` is a complete initializer for the extensions being added. To merge it into the project's own: explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, because a plugin can only be initialized once and a call with parameters has to get in before discovery does. Calls after `InitializePlugins()` stay after it.

It also changes the initialization of SqlServer, which the project already has. The comment above each of those calls says what to replace.

```cs
using System.Runtime.CompilerServices;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.SqlServer is already in the project, and the extensions being added
        // change how it has to be initialized. Replace its existing call with this one, or, where the
        // project relies on InitializePlugins() to enable it, add this call above that.
        // Verify.SqlServer: schema snapshots stay, but recording is off, because
        // Verify.EntityFramework records the same commands under the name ef, with the command
        // type and the transaction state.
        // This has to run before InitializePlugins(): discovery would otherwise initialize
        // Verify.SqlServer with recording on, and a second Initialize throws "Already Initialized".
        VerifySqlServer.Initialize(recordCommands: false);

        // Verify.EntityFramework: the model is read once here so IgnoreNavigationProperties() can be
        // called without arguments, and so navigation properties stay out of entity snapshots.
        VerifyEntityFramework.Initialize(GetDbModel());

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }

    // Builds a context against a connection string that is never opened, purely to read the model.
    static IModel GetDbModel()
    {
        var options = new DbContextOptionsBuilder<SampleDbContext>();
        options.UseSqlServer("fake");
        using var data = new SampleDbContext(options.Options);
        return data.Model;
    }
}
```

## 4. Add the tests

Copy `Extensions/` into the test project. Each file shows one extension, with a comment above every test saying what it demonstrates.

The tests exercise sample types in `src-samples/`: a DbContext, a controller, a message handler. They stand in for the project's own types. Either copy them into a project the tests reference, or point the tests at the real ones.

## Extensions

Each extension below teaches Verify about one kind of value: how to serialize it, how to compare two of them, or how to turn it into something readable. They are independent; removing one changes nothing else.

### Verify.EntityFramework (EF Core)

Records the SQL EF Core runs, snapshots ChangeTracker state, and turns an IQueryable into readable SQL. ([repository](https://github.com/VerifyTests/Verify.EntityFramework))

Packages: `Verify.EntityFramework` 15.5.0, `Microsoft.EntityFrameworkCore.SqlServer` 10.0.12 (the provider the sample DbContext uses).

Enabled by:

```cs
VerifyEntityFramework.Initialize(GetDbModel());
```

`Extensions/EntityFrameworkTests.cs` in the download shows every documented API:

**RecordCommands** — Everything EF runs between Recording.Start() and the verification is added to the snapshot under the name ef, with the command text, its type and the transaction state. Nothing needs to be passed to Verify: recorded entries are included on their own.

```cs
await using var data = NewContext();
Recording.Start();

await data
    .Companies
    .Where(_ => _.Name == "Title")
    .ToListAsync();

await Verify();
```

**Queryable** — Verifying an IQueryable writes two snapshots: a .verified.txt with the materialized results, and a .verified.sql with the SQL EF would run. The query is not executed for the SQL file, so a broken query still shows up as a diff rather than as an exception.

```cs
await using var data = NewContext();
var queryable = data
    .Companies
    .Where(_ => _.Name == "Title");
await Verify(queryable);
```

**ChangeTracker** — A ChangeTracker snapshot lists the added, modified and deleted entities, and for a modified one both the original and the current value of each changed property.

```cs
await using var data = NewContext();
var company = new Company
{
    Name = "new name"
};
data.Add(company);
await Verify(data.ChangeTracker);
```

**IgnoreNavigationProperties** — Navigation properties make an entity snapshot recursive and noisy. This drops them, using the model captured in the module initializer.

```cs
var employee = new Employee
{
    Name = "John"
};
return Verify(employee)
    .IgnoreNavigationProperties();
```

**StopRecording** — Recording.Stop() returns the entries instead of adding them, so they can be filtered or combined with other values before being verified.

```cs
await using var data = NewContext();
Recording.Start();

var count = await data.Companies.CountAsync();

var entries = Recording.Stop();
await Verify(
    new
    {
        count,
        entries
    });
```

**DescriptiveSql** — EF names tables c and e, and parameters @p0 and @p1. These two replace them with the table and column names, so a change in the query is visible in the diff.

```cs
var builder = new DbContextOptionsBuilder<SampleDbContext>();
builder.UseSqlServer("Server=(local);Database=Sample;Integrated Security=true;Encrypt=false");
builder.EnableRecording();
builder.UseDescriptiveTableAliases();
builder.UseDescriptiveParameterNames();
await using var data = new SampleDbContext(builder.Options);

Recording.Start();
await data.Companies.Where(_ => _.Name == "Title").ToListAsync();
await Verify();
```

**ScrubDateTimes** — Some queries, such as those against temporal tables, have EF inline a DateTime into the SQL. This replaces each one with a stable token.

```cs
await using var data = NewContext();
Recording.Start();

await data.Companies.ToListAsync();

await Verify()
    .ScrubInlineEfDateTimes();
```

Notes:

 * `EnableRecording()` belongs in test code only: it attaches an interceptor to every command.
 * `Recording.Start()` can be called on several contexts built from the same options, and the entries are aggregated.
 * Testing through `WebApplicationFactory` needs a named recording, `Recording.Start(name)` and `Recording.Stop(name)`, and the entries have to be passed to Verify explicitly.
 * The samples point at a local SQL Server. Selecting LocalDb as well generates a database per test instead.

## Interactions between the selected extensions

### Warning: EntityFramework, SqlServer

Verify.EntityFramework and Verify.SqlServer both record every command EF Core runs: Verify.EntityFramework under the name ef, with the command type and transaction state, and Verify.SqlServer under the name sql. Without a choice here every command appears twice in each snapshot.

Which extension records EF commands: **Verify.EntityFramework records them**. The alternatives:

 * **Verify.EntityFramework records them** — Adds the command type and transaction state. Verify.SqlServer still snapshots schema.
 * **Verify.SqlServer records them** — Records every SqlCommand, including ones EF did not issue.
 * **Both record, the sql entries are dropped** — Recording.IgnoreNames("sql") needs no ordering, but every command is still cloned before being discarded.
 * **Both record, both appear** — Every EF command appears twice in the snapshot.

 * The extension that stops recording is initialized with `recordCommands: false`, which has to happen before `VerifierSettings.InitializePlugins()`: discovery would otherwise initialize it with recording on, and the second call throws "Already Initialized".
 * Verify.SqlServer's schema snapshots are unaffected by this choice.

### Info: EntityFramework, SqlServer

These extensions all append to the same recording, which lands in every snapshot taken while it is running, each entry under its own name: ef, sql, httpCall, activity and log.

 * `Recording.Start()` begins one; `Recording.Stop()` returns the entries instead of adding them to the snapshot.
 * `Recording.IgnoreNames("sql")` drops one kind of entry everywhere, but the extension still produces it first.
 * Every logging extension records under `log`, so `IgnoreNames` cannot separate them (plan A12).

## Running the tests

A new test has no `.verified.` file, so its first run fails and writes a `.received.` file. Review it, then accept it by renaming it to `.verified.`, accepting it in the diff tool, DiffEngineTray or IDE plugin, or running `dotnet verify accept`.

Set the environment variable `DiffEngine_Disabled=true` when running tests where a diff tool should not open, such as a build server or an AI assistant's terminal. Verify detects the common build servers itself.

## Further reading

 * [Verify readme](https://github.com/VerifyTests/Verify#readme)
 * [Serializer settings](https://github.com/VerifyTests/Verify/blob/main/docs/serializer-settings.md)
 * [Scrubbers](https://github.com/VerifyTests/Verify/blob/main/docs/scrubbers.md)
 * [File naming](https://github.com/VerifyTests/Verify/blob/main/docs/naming.md)
 * [Parameterised tests](https://github.com/VerifyTests/Verify/blob/main/docs/parameterised.md)
 * [Plugins](https://github.com/VerifyTests/Verify/blob/main/docs/plugins.md)
 * [Using Verify with AI assistants](https://github.com/VerifyTests/Verify/blob/main/docs/ai-usage.md)

==== CLAUDE.md

# Adding Verify extensions: instructions for AI coding assistants

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=DiffPlex,SqlServer&ext=EntityFramework). The files next to this one are changes to merge into a project that already uses Verify snapshot testing with xUnit v3. Carry out the steps below in order.

## Steps

1. If the solution uses Central Package Management (a `Directory.Packages.props` with `ManagePackageVersionsCentrally`), merge the `PackageVersion` items from `Directory.Packages.props.fragment.xml` into it. Otherwise give each `PackageReference` in the next step the version listed in that file.
2. Merge `TestProject.csproj.fragment.xml` into the test project: the `PackageReference` items, and any properties and items it has.
3. Merge `ModuleInitializer.cs` into the test project's existing module initializer. Explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, in the order given: a plugin can only be initialized once, so a call with parameters has to come before discovery. Calls after `InitializePlugins()` stay after it. If the project has no module initializer, add the file as it is.
4. Verify.SqlServer is already in the project. Replace its existing initialization with `VerifySqlServer.Initialize(recordCommands: false);`, or, if the project relies on `InitializePlugins()` to enable it, add that call above `InitializePlugins()`.
5. Copy `Extensions/` into the test project.
6. The tests use placeholder types from `src-samples/`. Prefer pointing the tests at the project's real types; otherwise copy the placeholders into a project the tests reference.
7. Build, then run the tests with `DiffEngine_Disabled=true`. The new tests fail on their first run because they have no `.verified.` files yet. Show the user each `.received.` file; accept one only once the user agrees it is right.

## Verify extensions in this project

### Verify.EntityFramework (EF Core)

 * Records the SQL EF Core runs, snapshots ChangeTracker state, and turns an IQueryable into readable SQL.
 * Repository: https://github.com/VerifyTests/Verify.EntityFramework
 * Enabled in `ModuleInitializer.cs` by `VerifyEntityFramework.Initialize(GetDbModel());`. Do not move that call after `VerifierSettings.InitializePlugins()`.
 * Samples: `Extensions/EntityFrameworkTests.cs`, using `RecordCommands`, `Queryable`, `ChangeTracker`, `IgnoreNavigationProperties`, `StopRecording`, `DescriptiveSql`, `ScrubDateTimes`.
 * Needs `using Microsoft.EntityFrameworkCore;` in the test file, not in global usings: extensions define colliding type names.
 * `EnableRecording()` belongs in test code only: it attaches an interceptor to every command.
 * `Recording.Start()` can be called on several contexts built from the same options, and the entries are aggregated.
 * Testing through `WebApplicationFactory` needs a named recording, `Recording.Start(name)` and `Recording.Stop(name)`, and the entries have to be passed to Verify explicitly.
 * The samples point at a local SQL Server. Selecting LocalDb as well generates a database per test instead.

## Decisions already taken

These follow from the combination of extensions. Changing one of them changes behaviour across the whole test project.

 * **EntityFramework, SqlServer**: Verify.EntityFramework and Verify.SqlServer both record every command EF Core runs: Verify.EntityFramework under the name ef, with the command type and transaction state, and Verify.SqlServer under the name sql. Without a choice here every command appears twice in each snapshot. In force: Verify.EntityFramework records them.
 * **EntityFramework, SqlServer**: These extensions all append to the same recording, which lands in every snapshot taken while it is running, each entry under its own name: ef, sql, httpCall, activity and log.

## Snapshot Testing (Verify)

This project uses [Verify](https://github.com/VerifyTests/Verify) for snapshot testing.

### Key concepts

- **`.verified.*` files** are the approved snapshots. They are committed to source control.
- **`.received.*` files** are the actual output from the latest test run. They are generated when a test fails (actual != expected) and are git-ignored.
- When a test fails, compare the `.received.*` file to the `.verified.*` file to understand the difference.

### Handling test failures

When a snapshot test fails:

1. Run the failing test and read the `.received.*` file that was generated.
2. Compare it to the corresponding `.verified.*` file.
3. Determine if the difference is expected (due to an intentional code change) or a bug.
   - **If expected**: copy the `.received.*` file to the `Verified:` path listed in the exception message to accept the new snapshot.
   - **If a bug**: fix the code, not the snapshot.
4. Never hand-edit `.verified.*` files to make tests pass. Always let Verify generate the correct output by running the test.
5. Never derive the verified name from the received name. The two are not always identical. Multi-targeted projects add a runtime and version suffix to the received file, and ignored parameters are kept in the received name but dropped from the verified name. Always take the destination from the `Verified:` line of the exception message.
6. Delete every `.verified.` file listed in the `Delete:` section. Those files are no longer produced by any test, usually because a test was renamed or removed.

If a snapshot differs between runs or between machines, add a scrubber to the test (`ScrubLinesContaining(...)`, `ScrubLines(...)`, `AddScrubber(...)`) rather than accepting the new output each time.

### Scrubbed values

Verify replaces non-deterministic values in snapshots with stable placeholders:

- GUIDs become `Guid_1`, `Guid_2`, etc.
- DateTimes become `DateTime_1`, `DateTime_2`, etc.
- File paths are replaced with tokens like `{SolutionDirectory}`, `{ProjectDirectory}`, `{TempPath}`.
- Custom scrubbers may replace other values.

These placeholders are intentional and ensure snapshots are deterministic across machines and runs.

### Exception message format

When a test fails, the exception message is machine-parsable with the following structure:

```
Directory: /path/to/test/project
New:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt
NotEqual:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt

FileContent:

New:

Received: TestClass.Method.received.txt
<file content here>

NotEqual:

Received: TestClass.Method.received.txt
<received content>
Verified: TestClass.Method.verified.txt
<verified content>
```

 * The `Directory:` line gives the base path for all relative file references.
 * `New` means no `.verified.` file exists yet (first run or new test).
 * `NotEqual` means the `.received.` and `.verified.` files differ.
 * `Delete` means a `.verified.` file is no longer produced by any test.
 * The `FileContent:` section contains the actual text content for quick comparison without needing to read files separately.

See [Exception Message Format](https://github.com/VerifyTests/Verify/blob/main/docs/exception-message-format.md) for full details.

### Verified file conventions

- Encoding: UTF-8 with BOM
- Line endings: LF (not CRLF)
- No trailing newline

### Common patterns

```csharp
// Verify an object (serialized to JSON)
await Verify(myObject);

// Verify a string
await Verify("some text output");

// Verify with inline settings
await Verify(myObject)
    .ScrubLines(line => line.Contains("volatile"))
    .DontScrubDateTimes();

// Verify throws
await ThrowsTask(() => MethodThatThrows())
    .IgnoreStackTrace();
```

==== Directory.Packages.props.fragment.xml

<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.EntityFramework" Version="15.5.0" />
  <PackageVersion Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.12" />
</ItemGroup>

==== TestProject.csproj.fragment.xml

<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.EntityFramework" />
  <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
</ItemGroup>

==== ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=DiffPlex,SqlServer&ext=EntityFramework
// Delete whatever is not needed.
using System.Runtime.CompilerServices;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.SqlServer is already in the project, and the extensions being added
        // change how it has to be initialized. Replace its existing call with this one, or, where the
        // project relies on InitializePlugins() to enable it, add this call above that.
        // Verify.SqlServer: schema snapshots stay, but recording is off, because
        // Verify.EntityFramework records the same commands under the name ef, with the command
        // type and the transaction state.
        // This has to run before InitializePlugins(): discovery would otherwise initialize
        // Verify.SqlServer with recording on, and a second Initialize throws "Already Initialized".
        VerifySqlServer.Initialize(recordCommands: false);

        // Verify.EntityFramework: the model is read once here so IgnoreNavigationProperties() can be
        // called without arguments, and so navigation properties stay out of entity snapshots.
        VerifyEntityFramework.Initialize(GetDbModel());

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }

    // Builds a context against a connection string that is never opened, purely to read the model.
    static IModel GetDbModel()
    {
        var options = new DbContextOptionsBuilder<SampleDbContext>();
        options.UseSqlServer("fake");
        using var data = new SampleDbContext(options.Options);
        return data.Model;
    }
}

==== Extensions/EntityFrameworkTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=DiffPlex,SqlServer&ext=EntityFramework
// Delete whatever is not needed.
using Microsoft.EntityFrameworkCore;

// Verify.EntityFramework (EF Core): Records the SQL EF Core runs, snapshots ChangeTracker state, and turns an IQueryable into readable SQL.
// https://github.com/VerifyTests/Verify.EntityFramework
// Note: EnableRecording() belongs in test code only: it attaches an interceptor to every command.
// Note: Recording.Start() can be called on several contexts built from the same options, and the
// entries are aggregated.
// Note: Testing through WebApplicationFactory needs a named recording, Recording.Start(name) and
// Recording.Stop(name), and the entries have to be passed to Verify explicitly.
// Note: The samples point at a local SQL Server. Selecting LocalDb as well generates a database
// per test instead.
public class EntityFrameworkTests
{
    // Everything EF runs between Recording.Start() and the verification is added to the
    // snapshot under the name ef, with the command text, its type and the transaction state.
    // Nothing needs to be passed to Verify: recorded entries are included on their own.
    [Fact]
    public async Task RecordCommands()
    {
        await using var data = NewContext();
        Recording.Start();

        await data
            .Companies
            .Where(_ => _.Name == "Title")
            .ToListAsync();

        await Verify();
    }

    // Verifying an IQueryable writes two snapshots: a .verified.txt with the materialized
    // results, and a .verified.sql with the SQL EF would run. The query is not executed for the
    // SQL file, so a broken query still shows up as a diff rather than as an exception.
    [Fact]
    public async Task Queryable()
    {
        await using var data = NewContext();
        var queryable = data
            .Companies
            .Where(_ => _.Name == "Title");
        await Verify(queryable);
    }

    // A ChangeTracker snapshot lists the added, modified and deleted entities, and for a
    // modified one both the original and the current value of each changed property.
    [Fact]
    public async Task ChangeTracker()
    {
        await using var data = NewContext();
        var company = new Company
        {
            Name = "new name"
        };
        data.Add(company);
        await Verify(data.ChangeTracker);
    }

    // Navigation properties make an entity snapshot recursive and noisy. This drops them,
    // using the model captured in the module initializer.
    [Fact]
    public Task IgnoreNavigationProperties()
    {
        var employee = new Employee
        {
            Name = "John"
        };
        return Verify(employee)
            .IgnoreNavigationProperties();
    }

    // Recording.Stop() returns the entries instead of adding them, so they can be filtered
    // or combined with other values before being verified.
    [Fact]
    public async Task StopRecording()
    {
        await using var data = NewContext();
        Recording.Start();

        var count = await data.Companies.CountAsync();

        var entries = Recording.Stop();
        await Verify(
            new
            {
                count,
                entries
            });
    }

    // EF names tables c and e, and parameters @p0 and @p1. These two replace them with the
    // table and column names, so a change in the query is visible in the diff.
    [Fact]
    public async Task DescriptiveSql()
    {
        var builder = new DbContextOptionsBuilder<SampleDbContext>();
        builder.UseSqlServer("Server=(local);Database=Sample;Integrated Security=true;Encrypt=false");
        builder.EnableRecording();
        builder.UseDescriptiveTableAliases();
        builder.UseDescriptiveParameterNames();
        await using var data = new SampleDbContext(builder.Options);

        Recording.Start();
        await data.Companies.Where(_ => _.Name == "Title").ToListAsync();
        await Verify();
    }

    // Some queries, such as those against temporal tables, have EF inline a DateTime into the
    // SQL. This replaces each one with a stable token.
    [Fact]
    public async Task ScrubDateTimes()
    {
        await using var data = NewContext();
        Recording.Start();

        await data.Companies.ToListAsync();

        await Verify()
            .ScrubInlineEfDateTimes();
    }

    // EnableRecording attaches the interceptor, and belongs only in test code.
    static SampleDbContext NewContext()
    {
        var builder = new DbContextOptionsBuilder<SampleDbContext>();
        builder.UseSqlServer("Server=(local);Database=Sample;Integrated Security=true;Encrypt=false");
        builder.EnableRecording();
        return new(builder.Options);
    }
}

==== src-samples/SampleDbContext.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=DiffPlex,SqlServer&ext=EntityFramework
// Delete whatever is not needed.
using Microsoft.EntityFrameworkCore;

// Stands in for a real DbContext. The samples record the SQL EF Core produces for it.
public class SampleDbContext(DbContextOptions<SampleDbContext> options) :
    DbContext(options)
{
    public DbSet<Company> Companies { get; set; } = null!;
    public DbSet<Employee> Employees { get; set; } = null!;
}

public class Company
{
    public int Id { get; set; }
    public string Name { get; set; } = null!;
    public IList<Employee> Employees { get; set; } = [];
}

public class Employee
{
    public int Id { get; set; }
    public int CompanyId { get; set; }
    public Company Company { get; set; } = null!;
    public string Name { get; set; } = null!;
}

