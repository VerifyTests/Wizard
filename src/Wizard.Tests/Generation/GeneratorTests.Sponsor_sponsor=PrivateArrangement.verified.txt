<Project>
  <!-- Applies to every project under this directory. -->
  <PropertyGroup>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable Condition="'$(MSBuildProjectExtension)' == '.csproj'">enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <!-- Open Source Maintenance Fee: every Verify package checks at build time that the build
       declares its fee status, and fails with SC021 when it does not. Nothing phones home.
       https://github.com/VerifyTests/Verify/blob/main/docs/maintenance-fee.md
       Other options, and the rules for each: https://simoncropp.github.io/SponsorCheck/package/Verify -->
  <!-- A private licensing arrangement, covered until the end of this month. Renew it with a one-line edit. -->
  <PropertyGroup>
    <Verify_SponsorshipLicensedUntil>2027-06</Verify_SponsorshipLicensedUntil>
  </PropertyGroup>
</Project>

Summary: Private arrangement

Outcome: The build passes silently through the end of 2027-06 (UTC). After that it fails with SC025 until the month is renewed; a month more than 12 months out fails with SC037.

Url: step=output&os=Windows&ide=Rider&cli=Cli&tf=XunitV3&ci=GitHubActions&sponsor=Private&until=2027-06