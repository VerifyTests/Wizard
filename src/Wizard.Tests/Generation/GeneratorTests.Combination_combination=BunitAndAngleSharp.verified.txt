==== selected

AngleSharp
Bunit
DiffPlex

==== interactions

Warning bunit-anglesharp-comparer [AngleSharp, Bunit]
Both register a string comparer for html. Verify.Bunit is initialized first, so Verify.AngleSharp's semantic comparison wins.

Info diffplex-default-comparer [AngleSharp, Bunit, DiffPlex]
Verify.DiffPlex is the default comparer for text snapshots. The extensions selected here register comparers for specific extensions (html, json, svg), which take precedence over it.

Info html-prettyprint [AngleSharp, Bunit]
Rendered html is a single long line, which makes snapshot diffs unreadable. `HtmlPrettyPrint.All()` formats every html snapshot, and Verify.AngleSharp compares them semantically rather than as text.

Info plugin-not-discovered [AngleSharp]
VerifierSettings.InitializePlugins() cannot find AngleSharp: it looks for a type named after the assembly, and silently skips an assembly that has none. The module initializer calls each of them explicitly, so they are enabled either way.

==== src/VerifySample.Tests/ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/new?step=output&os=Windows&ide=Rider&cli=Cli&tf=XunitV3&ci=GitHubActions&ext=AngleSharp,Bunit,DiffPlex&sponsor=Exempt&exempt=OpenSource&until=2027-09
// Delete whatever is not needed.
using System.Runtime.CompilerServices;
using VerifyTests.AngleSharp;
using VerifyTests.DiffPlex;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.DiffPlex: when a text snapshot does not match, the failure message shows an inline
        // diff instead of the whole received and verified text.
        // OutputType.Compact prints only the changed lines, with a line of context either side.
        // Alternatives: OutputType.Full, OutputType.Minimal.
        VerifyDiffPlex.Initialize(OutputType.Compact);

        // Verify.Bunit: renders a component through bUnit and writes its markup as an html snapshot.
        // excludeComponent decides whether the component's own state is written alongside the markup.
        VerifyBunit.Initialize(excludeComponent: false);

        // Verify.AngleSharp: compares html, htm and svg by parsing both sides and diffing the DOM, so
        // reordered attributes, reformatted markup and whitespace no longer fail a test.
        // The call is explicit because the class is named VerifyAngleSharpDiffing, and plugin discovery
        // looks for a type named after the assembly, VerifyAngleSharp, so it never finds this one.
        VerifyAngleSharpDiffing.Initialize();
        // VerifyAngleSharpDiffing.Initialize(_ => _.AddDefaultOptions().AddFilter(SpanFilter));

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();

        // Formats every html and htm snapshot before it is written, so a rendered page is a
        // readable tree rather than one long line. Takes an optional action for scrubbing the
        // nodes, for example _ => _.ScrubAttributes("id").
        HtmlPrettyPrint.All();
    }
}


==== files

global.json
Directory.Build.props
Directory.Packages.props
nuget.config
.gitignore
.gitattributes
.editorconfig
VerifySample.slnx
VerifySample.slnx.DotSettings
.config/dotnet-tools.json
readme.md
CLAUDE.md
.github/copilot-instructions.md
.claude/skills/verify-snapshot-testing/SKILL.md
.github/workflows/build.yml
src/VerifySample/VerifySample.csproj
src/VerifySample/ClassBeingTested.cs
src/VerifySample/SampleModels.cs
src/VerifySample/BunitComponent.cs
src/VerifySample.Tests/VerifySample.Tests.csproj
src/VerifySample.Tests/Sample.Test.verified.txt
src/VerifySample.Tests/ModuleInitializer.cs
src/VerifySample.Tests/Sample.cs
src/VerifySample.Tests/VerifyChecksTests.cs
src/VerifySample.Tests/Extensions/AngleSharpTests.cs
src/VerifySample.Tests/Extensions/BunitTests.cs
src/VerifySample.Tests/Extensions/DiffPlexTests.cs
src/VerifySample.Tests/Extensions/DiffPlexTests.PerTestOutput.verified.txt