==== interactions



==== readme.md

# Adding Verify extensions

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=Http&sponsor=Exempt&exempt=SmallRevenue&until=2027-09) (reopen that link to change any answer).

 * Test framework: xUnit v3
 * Adding: Http (verbose)
 * Already in the project: none listed
 * Maintenance fee: Exempt: SmallRevenue

The download holds the changes below as files to merge, each named for where it goes. `CLAUDE.md` has the same steps written for an AI assistant to carry out.

## 1. Add the packages

With Central Package Management, merge `Directory.Packages.props.fragment.xml` into `Directory.Packages.props`, and the `PackageReference` items in `TestProject.csproj.fragment.xml` into the test project:

```xml
<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.Http" Version="7.5.1" />
</ItemGroup>
```

Without it, reference each package with its version instead:

```xml
<PackageReference Include="Verify.Http" Version="7.5.1" />
```

Or from the command line, in the test project's directory:

```
dotnet add package Verify.Http
```

## 2. Update the test project

`TestProject.csproj.fragment.xml` has everything the test project gains:

```xml
<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.Http" />
</ItemGroup>
```

## 3. Update the module initializer

`ModuleInitializer.cs` is a complete initializer for the extensions being added. To merge it into the project's own: explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, because a plugin can only be initialized once and a call with parameters has to get in before discovery does. Calls after `InitializePlugins()` stay after it.

```cs
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.Http: requests, responses, headers and content are written as readable snapshots,
        // and calls made while a recording is running are appended under the name httpCall.
        // Called explicitly so it runs before Verify.Flurl, which initializes Verify.Http itself
        // when it has not already run; a later VerifyHttp.Initialize() throws "Already Initialized".
        VerifyHttp.Initialize();

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}
```

## 4. Add the tests

Copy `Extensions/` into the test project. Each file shows one extension, with a comment above every test saying what it demonstrates.

## Extensions

Each extension below teaches Verify about one kind of value: how to serialize it, how to compare two of them, or how to turn it into something readable. They are independent; removing one changes nothing else.

### Verify.Http

Snapshots http requests and responses, records what an HttpClient sent, and ships a mock HttpClient. ([repository](https://github.com/VerifyTests/Verify.Http))

Package: `Verify.Http` 7.5.1.

Enabled by:

```cs
VerifyHttp.Initialize();
```

`Extensions/HttpTests.cs` in the download shows every documented API:

**MockResponse** — MockHttpClient answers every request without touching the network, so the test is deterministic. The response is verified as status, headers and content.

```cs
using var client = new MockHttpClient("the response", "text/plain");

var result = await client.GetAsync("https://fake/get");

await Verify(result);
```

**RecordCalls** — Every call made between Recording.Start() and the verification is added to the snapshot under the name httpCall, so nothing has to be passed to Verify.

```cs
using var client = new MockHttpClient(recording: true);
Recording.Start();

await client.GetAsync("https://fake/get");

await Verify();
```

**ScrubResponseBody** — ScrubHttpTextResponse rewrites a text response body before it is written, for values that change on every run.

```cs
using var client = new MockHttpClient("Herman Melville - Moby-Dick", "text/plain");

var result = await client.GetAsync("https://fake/get");

await Verify(result)
    .ScrubHttpTextResponse(_ => _.Replace("Herman Melville - Moby-Dick", "New title"));
```

**IgnoreHeaders** — Headers are serialized as members, so a header that varies per run is dropped with the same IgnoreMembers that drops any other member.

```cs
using var client = new MockHttpClient();

var result = await client.GetAsync("https://fake/get");

await Verify(result)
    .IgnoreMembers(
        "Server",
        "Content-Length",
        "Date");
```

**RecordingHttpClientFactory** — AddRecordingHttpClient adds a client to the container together with a handler that keeps every call it made, which is how a service built by dependency injection is covered. The handler is instance based, so it needs no recording and no ordering.

```cs
var collection = new ServiceCollection();
var (builder, recording) = collection.AddRecordingHttpClient();
builder.ConfigurePrimaryHttpMessageHandler(() => new MockHttpHandler("the response", "text/plain"));

await using var provider = collection.BuildServiceProvider();
var factory = provider.GetRequiredService<IHttpClientFactory>();
using var client = factory.CreateClient(builder.Name);

await client.GetAsync("https://fake/get");

await Verify(recording.Sends)
    .IgnoreMember("Date");
```

**SimulateNetworkStream** — A real network response is a read once, non seekable stream. This reproduces that, so code that reads the body twice fails in the test rather than in production.

```cs
using var client = new MockHttpClient("the response", "text/plain")
{
    SimulateNetworkStream = true
};

var result = await client.GetAsync(
    "https://fake/get",
    HttpCompletionOption.ResponseHeadersRead);

await Verify(result);
```

**FilterRecordedCalls** — Recording.Stop() returns the entries instead of adding them, so they can be filtered or asserted on before being verified. An HttpCall carries the request, response and status.

```cs
using var client = new MockHttpClient(recording: true);
Recording.Start();

await client.GetAsync("https://fake/get");

var httpCalls = Recording.Stop()
    .Select(_ => _.Data)
    .OfType<HttpCall>()
    .ToList();
await Verify(httpCalls);
```

Notes:

 * Recorded http entries are named `httpCall`; `Recording.IgnoreNames("http")` matches nothing.
 * The readme's `HttpRecording.StartRecording()` does not exist; the API is `Recording.Start()` from Verify core.
 * Nothing is captured unless a recording is running, so `Recording.Start()` has to run before the code under test makes its calls.
 * `MockHttpClient` can also replay files, `new MockHttpClient("sample.html", "sample.json")`, with the files copied to the output directory.
 * `MockHttpHandler` has the same constructors, for when the handler rather than the client has to be injected.
 * The package targets net10.0 only.

## Open Source Maintenance Fee

Merge `Directory.Build.props.fragment.xml` into `Directory.Build.props`:

```xml
<!-- Merge into Directory.Build.props, replacing any existing Verify_ declaration. -->
<!-- Open Source Maintenance Fee: every Verify package checks at build time that the build
     declares its fee status, and fails with SC021 when it does not. Nothing phones home.
     https://github.com/VerifyTests/Verify/blob/main/docs/maintenance-fee.md
     Other options, and the rules for each: https://simoncropp.github.io/SponsorCheck/package/Verify -->
<!-- Exempt: an individual, or an organization with annual gross revenue under US$10,000 (government agencies excluded). The claim is time-bounded: renew the month before it passes. -->
<PropertyGroup>
  <Verify_SponsorshipExemption>SmallRevenue</Verify_SponsorshipExemption>
  <Verify_SponsorshipExemptionUntil>2027-09</Verify_SponsorshipExemptionUntil>
</PropertyGroup>
```

## Running the tests

A new test has no `.verified.` file, so its first run fails and writes a `.received.` file. Review it, then accept it by renaming it to `.verified.`, accepting it in the diff tool, DiffEngineTray or IDE plugin, or running `dotnet verify accept`.

Set the environment variable `DiffEngine_Disabled=true` when running tests where a diff tool should not open, such as a build server or an AI assistant's terminal. Verify detects the common build servers itself.

## Further reading

 * [Verify readme](https://github.com/VerifyTests/Verify#readme)
 * [Serializer settings](https://github.com/VerifyTests/Verify/blob/main/docs/serializer-settings.md)
 * [Scrubbers](https://github.com/VerifyTests/Verify/blob/main/docs/scrubbers.md)
 * [File naming](https://github.com/VerifyTests/Verify/blob/main/docs/naming.md)
 * [Parameterised tests](https://github.com/VerifyTests/Verify/blob/main/docs/parameterised.md)
 * [Plugins](https://github.com/VerifyTests/Verify/blob/main/docs/plugins.md)
 * [Using Verify with AI assistants](https://github.com/VerifyTests/Verify/blob/main/docs/ai-usage.md)

==== CLAUDE.md

# Adding Verify extensions: instructions for AI coding assistants

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=Http&sponsor=Exempt&exempt=SmallRevenue&until=2027-09). The files next to this one are changes to merge into a project that already uses Verify snapshot testing with xUnit v3. Carry out the steps below in order.

## Steps

1. If the solution uses Central Package Management (a `Directory.Packages.props` with `ManagePackageVersionsCentrally`), merge the `PackageVersion` items from `Directory.Packages.props.fragment.xml` into it. Otherwise give each `PackageReference` in the next step the version listed in that file.
2. Merge `TestProject.csproj.fragment.xml` into the test project: the `PackageReference` items, and any properties and items it has.
3. Merge `ModuleInitializer.cs` into the test project's existing module initializer. Explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, in the order given: a plugin can only be initialized once, so a call with parameters has to come before discovery. Calls after `InitializePlugins()` stay after it. If the project has no module initializer, add the file as it is.
4. Copy `Extensions/` into the test project.
5. Merge `Directory.Build.props.fragment.xml` into `Directory.Build.props`. A block that is commented out is a decision for the user; do not choose one of its options on their behalf.
6. Build, then run the tests with `DiffEngine_Disabled=true`. The new tests fail on their first run because they have no `.verified.` files yet. Show the user each `.received.` file; accept one only once the user agrees it is right.

## Verify extensions in this project

### Verify.Http

 * Snapshots http requests and responses, records what an HttpClient sent, and ships a mock HttpClient.
 * Repository: https://github.com/VerifyTests/Verify.Http
 * Enabled in `ModuleInitializer.cs` by `VerifyHttp.Initialize();`. Do not move that call after `VerifierSettings.InitializePlugins()`.
 * Samples: `Extensions/HttpTests.cs`, using `MockResponse`, `RecordCalls`, `ScrubResponseBody`, `IgnoreHeaders`, `RecordingHttpClientFactory`, `SimulateNetworkStream`, `FilterRecordedCalls`.
 * Needs `using Microsoft.Extensions.DependencyInjection;`, `using VerifyTests.Http;` in the test file, not in global usings: extensions define colliding type names.
 * Recorded http entries are named `httpCall`; `Recording.IgnoreNames("http")` matches nothing.
 * The readme's `HttpRecording.StartRecording()` does not exist; the API is `Recording.Start()` from Verify core.
 * Nothing is captured unless a recording is running, so `Recording.Start()` has to run before the code under test makes its calls.
 * `MockHttpClient` can also replay files, `new MockHttpClient("sample.html", "sample.json")`, with the files copied to the output directory.
 * `MockHttpHandler` has the same constructors, for when the handler rather than the client has to be injected.
 * The package targets net10.0 only.

## Snapshot Testing (Verify)

This project uses [Verify](https://github.com/VerifyTests/Verify) for snapshot testing.

### Key concepts

- **`.verified.*` files** are the approved snapshots. They are committed to source control.
- **`.received.*` files** are the actual output from the latest test run. They are generated when a test fails (actual != expected) and are git-ignored.
- When a test fails, compare the `.received.*` file to the `.verified.*` file to understand the difference.

### Handling test failures

When a snapshot test fails:

1. Run the failing test and read the `.received.*` file that was generated.
2. Compare it to the corresponding `.verified.*` file.
3. Determine if the difference is expected (due to an intentional code change) or a bug.
   - **If expected**: copy the `.received.*` file to the `Verified:` path listed in the exception message to accept the new snapshot.
   - **If a bug**: fix the code, not the snapshot.
4. Never hand-edit `.verified.*` files to make tests pass. Always let Verify generate the correct output by running the test.
5. Never derive the verified name from the received name. The two are not always identical. Multi-targeted projects add a runtime and version suffix to the received file, and ignored parameters are kept in the received name but dropped from the verified name. Always take the destination from the `Verified:` line of the exception message.
6. Delete every `.verified.` file listed in the `Delete:` section. Those files are no longer produced by any test, usually because a test was renamed or removed.

If a snapshot differs between runs or between machines, add a scrubber to the test (`ScrubLinesContaining(...)`, `ScrubLines(...)`, `AddScrubber(...)`) rather than accepting the new output each time.

### Scrubbed values

Verify replaces non-deterministic values in snapshots with stable placeholders:

- GUIDs become `Guid_1`, `Guid_2`, etc.
- DateTimes become `DateTime_1`, `DateTime_2`, etc.
- File paths are replaced with tokens like `{SolutionDirectory}`, `{ProjectDirectory}`, `{TempPath}`.
- Custom scrubbers may replace other values.

These placeholders are intentional and ensure snapshots are deterministic across machines and runs.

### Exception message format

When a test fails, the exception message is machine-parsable with the following structure:

```
Directory: /path/to/test/project
New:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt
NotEqual:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt

FileContent:

New:

Received: TestClass.Method.received.txt
<file content here>

NotEqual:

Received: TestClass.Method.received.txt
<received content>
Verified: TestClass.Method.verified.txt
<verified content>
```

 * The `Directory:` line gives the base path for all relative file references.
 * `New` means no `.verified.` file exists yet (first run or new test).
 * `NotEqual` means the `.received.` and `.verified.` files differ.
 * `Delete` means a `.verified.` file is no longer produced by any test.
 * The `FileContent:` section contains the actual text content for quick comparison without needing to read files separately.

See [Exception Message Format](https://github.com/VerifyTests/Verify/blob/main/docs/exception-message-format.md) for full details.

### Verified file conventions

- Encoding: UTF-8 with BOM
- Line endings: LF (not CRLF)
- No trailing newline

### Common patterns

```csharp
// Verify an object (serialized to JSON)
await Verify(myObject);

// Verify a string
await Verify("some text output");

// Verify with inline settings
await Verify(myObject)
    .ScrubLines(line => line.Contains("volatile"))
    .DontScrubDateTimes();

// Verify throws
await ThrowsTask(() => MethodThatThrows())
    .IgnoreStackTrace();
```

==== Directory.Packages.props.fragment.xml

<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.Http" Version="7.5.1" />
</ItemGroup>

==== TestProject.csproj.fragment.xml

<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.Http" />
</ItemGroup>

==== ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=Http&sponsor=Exempt&exempt=SmallRevenue&until=2027-09
// Delete whatever is not needed.
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.Http: requests, responses, headers and content are written as readable snapshots,
        // and calls made while a recording is running are appended under the name httpCall.
        // Called explicitly so it runs before Verify.Flurl, which initializes Verify.Http itself
        // when it has not already run; a later VerifyHttp.Initialize() throws "Already Initialized".
        VerifyHttp.Initialize();

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}

==== Directory.Build.props.fragment.xml

<!-- Merge into Directory.Build.props, replacing any existing Verify_ declaration. -->
<!-- Open Source Maintenance Fee: every Verify package checks at build time that the build
     declares its fee status, and fails with SC021 when it does not. Nothing phones home.
     https://github.com/VerifyTests/Verify/blob/main/docs/maintenance-fee.md
     Other options, and the rules for each: https://simoncropp.github.io/SponsorCheck/package/Verify -->
<!-- Exempt: an individual, or an organization with annual gross revenue under US$10,000 (government agencies excluded). The claim is time-bounded: renew the month before it passes. -->
<PropertyGroup>
  <Verify_SponsorshipExemption>SmallRevenue</Verify_SponsorshipExemption>
  <Verify_SponsorshipExemptionUntil>2027-09</Verify_SponsorshipExemptionUntil>
</PropertyGroup>

==== Extensions/HttpTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&ext=Http&sponsor=Exempt&exempt=SmallRevenue&until=2027-09
// Delete whatever is not needed.
using Microsoft.Extensions.DependencyInjection;
using VerifyTests.Http;

// Verify.Http: Snapshots http requests and responses, records what an HttpClient sent, and ships a mock HttpClient.
// https://github.com/VerifyTests/Verify.Http
// Note: Recorded http entries are named httpCall; Recording.IgnoreNames("http") matches nothing.
// Note: The readme's HttpRecording.StartRecording() does not exist; the API is Recording.Start()
// from Verify core.
// Note: Nothing is captured unless a recording is running, so Recording.Start() has to run before
// the code under test makes its calls.
// Note: MockHttpClient can also replay files, new MockHttpClient("sample.html", "sample.json"),
// with the files copied to the output directory.
// Note: MockHttpHandler has the same constructors, for when the handler rather than the client has
// to be injected.
// Note: The package targets net10.0 only.
public class HttpTests
{
    // MockHttpClient answers every request without touching the network, so the test is
    // deterministic. The response is verified as status, headers and content.
    [Fact]
    public async Task MockResponse()
    {
        using var client = new MockHttpClient("the response", "text/plain");

        var result = await client.GetAsync("https://fake/get");

        await Verify(result);
    }

    // Every call made between Recording.Start() and the verification is added to the snapshot
    // under the name httpCall, so nothing has to be passed to Verify.
    [Fact]
    public async Task RecordCalls()
    {
        using var client = new MockHttpClient(recording: true);
        Recording.Start();

        await client.GetAsync("https://fake/get");

        await Verify();
    }

    // ScrubHttpTextResponse rewrites a text response body before it is written, for values
    // that change on every run.
    [Fact]
    public async Task ScrubResponseBody()
    {
        using var client = new MockHttpClient("Herman Melville - Moby-Dick", "text/plain");

        var result = await client.GetAsync("https://fake/get");

        await Verify(result)
            .ScrubHttpTextResponse(_ => _.Replace("Herman Melville - Moby-Dick", "New title"));
    }

    // Headers are serialized as members, so a header that varies per run is dropped with the
    // same IgnoreMembers that drops any other member.
    [Fact]
    public async Task IgnoreHeaders()
    {
        using var client = new MockHttpClient();

        var result = await client.GetAsync("https://fake/get");

        await Verify(result)
            .IgnoreMembers(
                "Server",
                "Content-Length",
                "Date");
    }

    // AddRecordingHttpClient adds a client to the container together with a handler that keeps
    // every call it made, which is how a service built by dependency injection is covered.
    // The handler is instance based, so it needs no recording and no ordering.
    [Fact]
    public async Task RecordingHttpClientFactory()
    {
        var collection = new ServiceCollection();
        var (builder, recording) = collection.AddRecordingHttpClient();
        builder.ConfigurePrimaryHttpMessageHandler(() => new MockHttpHandler("the response", "text/plain"));

        await using var provider = collection.BuildServiceProvider();
        var factory = provider.GetRequiredService<IHttpClientFactory>();
        using var client = factory.CreateClient(builder.Name);

        await client.GetAsync("https://fake/get");

        await Verify(recording.Sends)
            .IgnoreMember("Date");
    }

    // A real network response is a read once, non seekable stream. This reproduces that, so
    // code that reads the body twice fails in the test rather than in production.
    [Fact]
    public async Task SimulateNetworkStream()
    {
        using var client = new MockHttpClient("the response", "text/plain")
        {
            SimulateNetworkStream = true
        };

        var result = await client.GetAsync(
            "https://fake/get",
            HttpCompletionOption.ResponseHeadersRead);

        await Verify(result);
    }

    // Recording.Stop() returns the entries instead of adding them, so they can be filtered or
    // asserted on before being verified. An HttpCall carries the request, response and status.
    [Fact]
    public async Task FilterRecordedCalls()
    {
        using var client = new MockHttpClient(recording: true);
        Recording.Start();

        await client.GetAsync("https://fake/get");

        var httpCalls = Recording.Stop()
            .Select(_ => _.Data)
            .OfType<HttpCall>()
            .ToList();
        await Verify(httpCalls);
    }
}

