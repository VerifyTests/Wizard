<Project>
  <!-- Applies to every project under this directory. -->
  <PropertyGroup>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable Condition="'$(MSBuildProjectExtension)' == '.csproj'">enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <!-- Open Source Maintenance Fee: every Verify package checks at build time that the build
       declares its fee status, and fails with SC021 when it does not. Nothing phones home.
       https://github.com/VerifyTests/Verify/blob/main/docs/maintenance-fee.md
       Other options, and the rules for each: https://simoncropp.github.io/SponsorCheck/package/Verify -->
  <!-- Sponsoring VerifyTests from this GitHub account, privately. A private sponsorship is never in the bundled sponsor list, so the claim is time-bounded: renew the month before it passes. -->
  <PropertyGroup>
    <Verify_GitHubSponsorAccount>acme</Verify_GitHubSponsorAccount>
    <Verify_SponsorshipPrivateUntil>2027-03</Verify_SponsorshipPrivateUntil>
  </PropertyGroup>
</Project>

Summary: Sponsor: acme

Outcome: Private sponsorships are never in the bundled sponsor list, so the declaration is trusted: the build passes through the end of 2027-03 (UTC) and logs an SC059 audit message. After that it fails with SC058 until the month is renewed; a month more than 12 months out fails with SC055.

Url: step=output&os=Windows&ide=Rider&cli=Cli&tf=XunitV3&ci=GitHubActions&sponsor=Sponsor&account=acme&private=2027-03