==== interactions



==== readme.md

# Adding Verify plugins

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=NUnit&ext=Terminal,WinForms) (reopen that link to change any answer).

 * Test framework: NUnit
 * Adding: Terminal (verbose), WinForms (verbose)
 * Already in the project: none listed
 * Maintenance fee: No change
 * Package versions: the newest stable releases as of 2026-09-22; nuget.org was not checked when this was generated

The download holds the changes below as files to merge, each named for where it goes. `CLAUDE.md` has the same steps written for an AI assistant to carry out.

## 1. Add the packages

With Central Package Management, merge `Directory.Packages.props.fragment.xml` into `Directory.Packages.props`, and the `PackageReference` items in `TestProject.csproj.fragment.xml` into the test project:

```xml
<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.WinForms" Version="1.0.0" />
</ItemGroup>
```

Without it, reference each package with its version instead:

```xml
<PackageReference Include="Verify.WinForms" Version="1.0.0" />
```

Or from the command line, in the test project's directory:

```
dotnet add package Verify.WinForms
```

## 2. Update the test project

`TestProject.csproj.fragment.xml` has everything the test project gains:

```xml
<!-- Merge into the test project. -->
<PropertyGroup>
  <!-- WinForms only runs on Windows: the test project has to
       target net10.0-windows, or these tests go in a separate project that does. -->
  <UseWindowsForms>true</UseWindowsForms>
</PropertyGroup>
<ItemGroup>
  <PackageReference Include="Verify.WinForms" />
</ItemGroup>
```

## 3. Update the module initializer

`ModuleInitializer.cs` is a complete initializer for the plugins being added. To merge it into the project's own: explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, because a plugin can only be initialized once and a call with parameters has to get in before discovery does. Calls after `InitializePlugins()` stay after it.

```cs
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}
```

## 4. Add the tests

Copy `Plugins/` into the test project. Each file shows one plugin, with a comment above every test saying what it demonstrates.

## Plugins

Each plugin below teaches Verify about one kind of value: how to serialize it, how to compare two of them, or how to turn it into something readable. They are independent; removing one changes nothing else.

### Verify.Terminal

A dotnet tool for reviewing, accepting and rejecting pending snapshots from the terminal. ([repository](https://github.com/VerifyTests/Verify.Terminal))

Enabled by `VerifierSettings.InitializePlugins()`, which needs no call of its own.

Notes:

 * The package id is `verify.tool`, not Verify.Terminal, and the command it installs is `dotnet verify`.
 * `dotnet verify review` walks each pending snapshot showing a diff; `dotnet verify accept` and `dotnet verify reject` take or drop them all, with `-y` to skip the prompts.
 * `-w` sets the working directory and `-c` the number of context lines a review shows.
 * The working directory has to contain the test project's `obj`: staged inline snapshots live there, so pointing `-w` at a snapshot folder alone finds nothing.
 * Inline snapshots are handled beside file snapshots; accepting one rewrites the literal in the source file rather than moving a file.
 * A pending inline snapshot is not written to disk while DiffEngineTray or a DiffEngineViewer owns the queue.
 * Two kinds cannot be accepted: snapshots that disagree between target frameworks, and ones whose call site has moved or gone.
 * Verify records which verified file each received file belongs to, so the pairing the tool shows is exact rather than guessed.

### Verify.WinForms

Renders a WinForms form, control or context menu to png and verifies the image. ([repository](https://github.com/VerifyTests/Verify.WinForms))

Package: `Verify.WinForms` 1.0.0.

Enabled by `VerifierSettings.InitializePlugins()`, which needs no call of its own.

`Plugins/WinFormsTests.cs` in the download shows every documented API:

**FormUsage** — The form is drawn to a bitmap and the png is the snapshot, so a change to the layout is reviewed as a picture rather than inferred from control properties.

```cs
return Verify(new SampleForm());
```

**ContextMenuStrip** — A menu has no surface of its own to draw on, so it gets a converter that hosts it first. Verifying it covers the item text and ordering a user actually sees.

```cs
var menu = new ContextMenuStrip();
var items = menu.Items;

items.Add(new ToolStripMenuItem("About"));
items.Add(new ToolStripMenuItem("Exit"));
return Verify(menu);
```

**UniqueForRuntime** — Rendering differs slightly between Windows versions and runtimes. A snapshot per runtime keeps a shared machine and a build agent from fighting over the same file.

```cs
return Verify(new SampleForm())
    .UniqueForRuntime();
```

Notes:

 * The only output is a png; there is no text snapshot, so an exact byte comparison is brittle. `VerifierSettings.UseSsimForPng()` or an image comparer plugin makes it tolerant.
 * Converters are registered for `Form`, `ContextMenuStrip`, `UserControl` and `Control`, each producing one png.
 * Showing a control installs a `WindowsFormsSynchronizationContext` on the thread, which would deadlock Verify's async IO, so the converter saves and restores it around the rendering.
 * Unlike Verify.Xaml, no STA apartment attribute is needed.
 * The sample form is nested in the test class: the generated class library is not a WinForms project, so a `Form` cannot live there.
 * WinForms and WPF define many of the same control names, so selecting Verify.Xaml as well makes `Label`, `Button` and their siblings ambiguous; the sample qualifies the one it uses.
 * Windows only: the test project targets `net10.0-windows` and sets `UseWindowsForms`.

## Running the tests

A new test has no `.verified.` file, so its first run fails and writes a `.received.` file. Review it, then accept it by renaming it to `.verified.`, accepting it in the diff tool, DiffEngineTray or IDE plugin, or running `dotnet verify accept`.

Set the environment variable `DiffEngine_Disabled=true` when running tests where a diff tool should not open, such as a build server or an AI assistant's terminal. Verify detects the common build servers itself.

## Further reading

 * [Verify readme](https://github.com/VerifyTests/Verify#readme)
 * [Serializer settings](https://github.com/VerifyTests/Verify/blob/main/docs/serializer-settings.md)
 * [Scrubbers](https://github.com/VerifyTests/Verify/blob/main/docs/scrubbers.md)
 * [File naming](https://github.com/VerifyTests/Verify/blob/main/docs/naming.md)
 * [Parameterised tests](https://github.com/VerifyTests/Verify/blob/main/docs/parameterised.md)
 * [Plugins](https://github.com/VerifyTests/Verify/blob/main/docs/plugins.md)
 * [Using Verify with AI assistants](https://github.com/VerifyTests/Verify/blob/main/docs/ai-usage.md)

==== CLAUDE.md

# Adding Verify plugins: instructions for AI coding assistants

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=NUnit&ext=Terminal,WinForms). The files next to this one are changes to merge into a project that already uses Verify snapshot testing with NUnit. Carry out the steps below in order.

## Steps

1. If the solution uses Central Package Management (a `Directory.Packages.props` with `ManagePackageVersionsCentrally`), merge the `PackageVersion` items from `Directory.Packages.props.fragment.xml` into it. Otherwise give each `PackageReference` in the next step the version listed in that file.
2. Merge `TestProject.csproj.fragment.xml` into the test project: the `PackageReference` items, and any properties and items it has.
3. Merge `ModuleInitializer.cs` into the test project's existing module initializer. Explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, in the order given: a plugin can only be initialized once, so a call with parameters has to come before discovery. Calls after `InitializePlugins()` stay after it. If the project has no module initializer, add the file as it is.
4. Copy `Plugins/` into the test project.
5. Merge `.config/dotnet-tools.json.fragment.json` into `.config/dotnet-tools.json`, then run `dotnet tool restore`.
6. Build, then run the tests with `DiffEngine_Disabled=true`. The new tests fail on their first run because they have no `.verified.` files yet. Show the user each `.received.` file; accept one only once the user agrees it is right.

## Verify plugins in this project

### Verify.Terminal

 * A dotnet tool for reviewing, accepting and rejecting pending snapshots from the terminal.
 * Repository: https://github.com/VerifyTests/Verify.Terminal
 * The package id is `verify.tool`, not Verify.Terminal, and the command it installs is `dotnet verify`.
 * `dotnet verify review` walks each pending snapshot showing a diff; `dotnet verify accept` and `dotnet verify reject` take or drop them all, with `-y` to skip the prompts.
 * `-w` sets the working directory and `-c` the number of context lines a review shows.
 * The working directory has to contain the test project's `obj`: staged inline snapshots live there, so pointing `-w` at a snapshot folder alone finds nothing.
 * Inline snapshots are handled beside file snapshots; accepting one rewrites the literal in the source file rather than moving a file.
 * A pending inline snapshot is not written to disk while DiffEngineTray or a DiffEngineViewer owns the queue.
 * Two kinds cannot be accepted: snapshots that disagree between target frameworks, and ones whose call site has moved or gone.
 * Verify records which verified file each received file belongs to, so the pairing the tool shows is exact rather than guessed.

### Verify.WinForms

 * Renders a WinForms form, control or context menu to png and verifies the image.
 * Repository: https://github.com/VerifyTests/Verify.WinForms
 * Samples: `Plugins/WinFormsTests.cs`, using `FormUsage`, `ContextMenuStrip`, `UniqueForRuntime`.
 * The only output is a png; there is no text snapshot, so an exact byte comparison is brittle. `VerifierSettings.UseSsimForPng()` or an image comparer plugin makes it tolerant.
 * Converters are registered for `Form`, `ContextMenuStrip`, `UserControl` and `Control`, each producing one png.
 * Showing a control installs a `WindowsFormsSynchronizationContext` on the thread, which would deadlock Verify's async IO, so the converter saves and restores it around the rendering.
 * Unlike Verify.Xaml, no STA apartment attribute is needed.
 * The sample form is nested in the test class: the generated class library is not a WinForms project, so a `Form` cannot live there.
 * WinForms and WPF define many of the same control names, so selecting Verify.Xaml as well makes `Label`, `Button` and their siblings ambiguous; the sample qualifies the one it uses.
 * Windows only: the test project targets `net10.0-windows` and sets `UseWindowsForms`.

## Snapshot Testing (Verify)

This project uses [Verify](https://github.com/VerifyTests/Verify) for snapshot testing.

### Key concepts

- **`.verified.*` files** are the approved snapshots. They are committed to source control.
- **`.received.*` files** are the actual output from the latest test run. They are generated when a test fails (actual != expected) and are git-ignored.
- When a test fails, compare the `.received.*` file to the `.verified.*` file to understand the difference.

### Handling test failures

When a snapshot test fails:

1. Run the failing test and read the `.received.*` file that was generated.
2. Compare it to the corresponding `.verified.*` file.
3. Determine if the difference is expected (due to an intentional code change) or a bug.
   - **If expected**: copy the `.received.*` file to the `Verified:` path listed in the exception message to accept the new snapshot.
   - **If a bug**: fix the code, not the snapshot.
4. Never hand-edit `.verified.*` files to make tests pass. Always let Verify generate the correct output by running the test.
5. Never derive the verified name from the received name. The two are not always identical. Multi-targeted projects add a runtime and version suffix to the received file, and ignored parameters are kept in the received name but dropped from the verified name. Always take the destination from the `Verified:` line of the exception message.
6. Delete every `.verified.` file listed in the `Delete:` section. Those files are no longer produced by any test, usually because a test was renamed or removed.

If a snapshot differs between runs or between machines, add a scrubber to the test (`ScrubLinesContaining(...)`, `ScrubLines(...)`, `AddScrubber(...)`) rather than accepting the new output each time.

### Scrubbed values

Verify replaces non-deterministic values in snapshots with stable placeholders:

- GUIDs become `Guid_1`, `Guid_2`, etc.
- DateTimes become `DateTime_1`, `DateTime_2`, etc.
- File paths are replaced with tokens like `{SolutionDirectory}`, `{ProjectDirectory}`, `{TempPath}`.
- Custom scrubbers may replace other values.

These placeholders are intentional and ensure snapshots are deterministic across machines and runs.

### Exception message format

When a test fails, the exception message is machine-parsable with the following structure:

```
Directory: /path/to/test/project
New:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt
NotEqual:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt

FileContent:

New:

Received: TestClass.Method.received.txt
<file content here>

NotEqual:

Received: TestClass.Method.received.txt
<received content>
Verified: TestClass.Method.verified.txt
<verified content>
```

 * The `Directory:` line gives the base path for all relative file references.
 * `New` means no `.verified.` file exists yet (first run or new test).
 * `NotEqual` means the `.received.` and `.verified.` files differ.
 * `Delete` means a `.verified.` file is no longer produced by any test.
 * The `FileContent:` section contains the actual text content for quick comparison without needing to read files separately.

See [Exception Message Format](https://github.com/VerifyTests/Verify/blob/main/docs/exception-message-format.md) for full details.

### Verified file conventions

- Encoding: UTF-8 with BOM
- Line endings: LF (not CRLF)
- No trailing newline

### Common patterns

```csharp
// Verify an object (serialized to JSON)
await Verify(myObject);

// Verify a string
await Verify("some text output");

// Verify with inline settings
await Verify(myObject)
    .ScrubLines(line => line.Contains("volatile"))
    .DontScrubDateTimes();

// Verify throws
await ThrowsTask(() => MethodThatThrows())
    .IgnoreStackTrace();
```

==== Directory.Packages.props.fragment.xml

<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.WinForms" Version="1.0.0" />
</ItemGroup>

==== TestProject.csproj.fragment.xml

<!-- Merge into the test project. -->
<PropertyGroup>
  <!-- WinForms only runs on Windows: the test project has to
       target net10.0-windows, or these tests go in a separate project that does. -->
  <UseWindowsForms>true</UseWindowsForms>
</PropertyGroup>
<ItemGroup>
  <PackageReference Include="Verify.WinForms" />
</ItemGroup>

==== ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=NUnit&ext=Terminal,WinForms
// Delete whatever is not needed.
using System.Runtime.CompilerServices;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();
    }
}

==== .config/dotnet-tools.json.fragment.json

{
  "tools": {
    "verify.tool": {
      "version": "1.0.0",
      "commands": [
        "dotnet-verify"
      ],
      "rollForward": false
    }
  }
}

==== Plugins/WinFormsTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=NUnit&ext=Terminal,WinForms
// Delete whatever is not needed.
// Verify.WinForms: Renders a WinForms form, control or context menu to png and verifies the image.
// https://github.com/VerifyTests/Verify.WinForms
// Note: The only output is a png; there is no text snapshot, so an exact byte comparison is
// brittle. VerifierSettings.UseSsimForPng() or an image comparer plugin makes it tolerant.
// Note: Converters are registered for Form, ContextMenuStrip, UserControl and Control, each
// producing one png.
// Note: Showing a control installs a WindowsFormsSynchronizationContext on the thread, which would
// deadlock Verify's async IO, so the converter saves and restores it around the rendering.
// Note: Unlike Verify.Xaml, no STA apartment attribute is needed.
// Note: The sample form is nested in the test class: the generated class library is not a WinForms
// project, so a Form cannot live there.
// Note: WinForms and WPF define many of the same control names, so selecting Verify.Xaml as well
// makes Label, Button and their siblings ambiguous; the sample qualifies the one it uses.
// Note: Windows only: the test project targets net10.0-windows and sets UseWindowsForms.
[TestFixture]
public class WinFormsTests
{
    // The form is drawn to a bitmap and the png is the snapshot, so a change to the layout is
    // reviewed as a picture rather than inferred from control properties.
    [Test]
    public Task FormUsage()
    {
        return Verify(new SampleForm());
    }

    // A menu has no surface of its own to draw on, so it gets a converter that hosts it first.
    // Verifying it covers the item text and ordering a user actually sees.
    [Test]
    public Task ContextMenuStrip()
    {
        var menu = new ContextMenuStrip();
        var items = menu.Items;

        items.Add(new ToolStripMenuItem("About"));
        items.Add(new ToolStripMenuItem("Exit"));
        return Verify(menu);
    }

    // Rendering differs slightly between Windows versions and runtimes. A snapshot per runtime
    // keeps a shared machine and a build agent from fighting over the same file.
    [Test]
    public Task UniqueForRuntime()
    {
        return Verify(new SampleForm())
            .UniqueForRuntime();
    }

    // Stands in for a form of your own. Nested here rather than in the class library, which
    // is not a WinForms project, so a Form cannot live there.
    class SampleForm :
        Form
    {
        public SampleForm()
        {
            Text = "Sample";
            ClientSize = new(300, 120);
            // Qualified because WPF defines a Label too, and selecting Verify.Xaml as well
            // puts both namespaces in the same project as global usings.
            Controls.Add(
                new System.Windows.Forms.Label
                {
                    Text = "Hello",
                    Location = new(10, 10),
                    AutoSize = true
                });
        }
    }
}

