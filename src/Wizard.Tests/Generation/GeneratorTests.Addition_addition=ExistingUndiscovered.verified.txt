==== interactions

Warning bunit-anglesharp-comparer [AngleSharp, Bunit]
Warning existing-not-discovered [AngleSharp]
Info html-prettyprint [AngleSharp, Bunit]

==== readme.md

# Adding Verify plugins

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=AngleSharp&ext=Bunit) (reopen that link to change any answer).

 * Test framework: xUnit v3
 * Adding: Bunit (verbose)
 * Already in the project: AngleSharp
 * Maintenance fee: No change
 * Package versions: the newest stable releases as of 2026-09-22; nuget.org was not checked when this was generated

The download holds the changes below as files to merge, each named for where it goes. `CLAUDE.md` has the same steps written for an AI assistant to carry out.

## 1. Add the packages

With Central Package Management, merge `Directory.Packages.props.fragment.xml` into `Directory.Packages.props`, and the `PackageReference` items in `TestProject.csproj.fragment.xml` into the test project:

```xml
<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.Bunit" Version="1.0.0" />
  <PackageVersion Include="bunit" Version="1.0.0" />
  <PackageVersion Include="Microsoft.AspNetCore.Components" Version="1.0.0" />
</ItemGroup>
```

Without it, reference each package with its version instead:

```xml
<PackageReference Include="Verify.Bunit" Version="1.0.0" />
<PackageReference Include="bunit" Version="1.0.0" />
<PackageReference Include="Microsoft.AspNetCore.Components" Version="1.0.0" />
```

Or from the command line, in the test project's directory:

```
dotnet add package Verify.Bunit
dotnet add package bunit
dotnet add package Microsoft.AspNetCore.Components
```

## 2. Update the test project

`TestProject.csproj.fragment.xml` has everything the test project gains:

```xml
<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.Bunit" />
  <PackageReference Include="bunit" />
  <PackageReference Include="Microsoft.AspNetCore.Components" />
</ItemGroup>
```

## 3. Update the module initializer

`ModuleInitializer.cs` is a complete initializer for the plugins being added. To merge it into the project's own: explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, because a plugin can only be initialized once and a call with parameters has to get in before discovery does. Calls after `InitializePlugins()` stay after it.

It also changes the initialization of AngleSharp, which the project already has. The comment above each of those calls says what to replace.

```cs
using System.Runtime.CompilerServices;
using VerifyTests.AngleSharp;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.Bunit: renders a component through bUnit and writes its markup as an html snapshot.
        // excludeComponent decides whether the component's own state is written alongside the markup.
        VerifyBunit.Initialize(excludeComponent: false);

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();

        // Formats every html and htm snapshot before it is written, so a rendered page is a
        // readable tree rather than one long line. Takes an optional action for scrubbing the
        // nodes, for example _ => _.ScrubAttributes("id").
        HtmlPrettyPrint.All();
    }
}
```

## 4. Add the tests

Copy `Plugins/` into the test project. Each file shows one plugin, with a comment above every test saying what it demonstrates.

The tests exercise sample types in `src-samples/`: a DbContext, a controller, a message handler. They stand in for the project's own types. Either copy them into a project the tests reference, or point the tests at the real ones.

## Plugins

Each plugin below teaches Verify about one kind of value: how to serialize it, how to compare two of them, or how to turn it into something readable. They are independent; removing one changes nothing else.

### Verify.Bunit

Snapshots a bUnit rendered Blazor component, with bUnit's full interaction api available first. ([repository](https://github.com/VerifyTests/Verify.Bunit))

Packages: `Verify.Bunit` 1.0.0, `bunit` 1.0.0 (the BunitContext the samples render through), `Microsoft.AspNetCore.Components` 1.0.0 (the ComponentBase the sample component derives from).

Enabled by:

```cs
VerifyBunit.Initialize(excludeComponent: false);
```

`Plugins/BunitTests.cs` in the download shows every documented API:

**Component** — The parameters are set through bUnit's builder, so they are checked against the component's properties at compile time rather than matched by name at run time.

```cs
using var context = new BunitContext();
var component = context.Render<BunitComponent>(
    _ =>
    {
        _.Add(parameter => parameter.Title, "The Title");
        _.Add(parameter => parameter.Name, "Sam");
    });
return Verify(component);
```

**Nodes** — Verifying the nodes, or a single node, snapshots only that markup: useful when a test is about one part of a page and the rest changes for unrelated reasons.

```cs
using var context = new BunitContext();
var component = context.Render<BunitComponent>(
    _ => _.Add(parameter => parameter.Title, "The Title"));
return Verify(component.Nodes);
```

Notes:

 * The samples use the bUnit v2 api, `BunitContext` and `context.Render<T>(...)`, not v1's `TestContext` and `RenderComponent`.
 * Verify.Bunit has no stable release on Verify 33 yet, so a prerelease version is referenced.
 * `VerifyBunit.Initialize()` registers bUnit's own markup comparer for html, which Verify.AngleSharp also does.
 * `component.WaitFor(...)` and `context.RenderComponentAndWait(...)` let a test verify a component that renders again after an async call.
 * The package suppresses BL0005 for the project that references it.

## Interactions between the selected plugins

### Warning: AngleSharp, Bunit

Both register a string comparer for html. Verify.Bunit is initialized first, so Verify.AngleSharp's semantic comparison wins.

Which html comparer wins: **Verify.AngleSharp**. The alternatives:

 * **Verify.AngleSharp** — Compares the DOM, so attribute order and whitespace do not fail a test.
 * **Verify.Bunit** — bUnit's own markup comparison.

### Warning: AngleSharp

VerifierSettings.InitializePlugins() cannot find AngleSharp: it looks for a type named after the assembly, and silently skips an assembly that has none. If the project relies on InitializePlugins() alone, they were never enabled, and each needs its own Initialize call in the module initializer.

### Info: AngleSharp, Bunit

Rendered html is a single long line, which makes snapshot diffs unreadable. `HtmlPrettyPrint.All()` formats every html snapshot, and Verify.AngleSharp compares them semantically rather than as text.

 * `HtmlPrettyPrint.All()` is in the `VerifyTests.AngleSharp` namespace, and runs after plugin discovery.

## Running the tests

A new test has no `.verified.` file, so its first run fails and writes a `.received.` file. Review it, then accept it by renaming it to `.verified.`, accepting it in the diff tool, DiffEngineTray or IDE plugin, or running `dotnet verify accept`.

Set the environment variable `DiffEngine_Disabled=true` when running tests where a diff tool should not open, such as a build server or an AI assistant's terminal. Verify detects the common build servers itself.

## Further reading

 * [Verify readme](https://github.com/VerifyTests/Verify#readme)
 * [Serializer settings](https://github.com/VerifyTests/Verify/blob/main/docs/serializer-settings.md)
 * [Scrubbers](https://github.com/VerifyTests/Verify/blob/main/docs/scrubbers.md)
 * [File naming](https://github.com/VerifyTests/Verify/blob/main/docs/naming.md)
 * [Parameterised tests](https://github.com/VerifyTests/Verify/blob/main/docs/parameterised.md)
 * [Plugins](https://github.com/VerifyTests/Verify/blob/main/docs/plugins.md)
 * [Using Verify with AI assistants](https://github.com/VerifyTests/Verify/blob/main/docs/ai-usage.md)

==== CLAUDE.md

# Adding Verify plugins: instructions for AI coding assistants

Generated by the [Verify wizard](https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=AngleSharp&ext=Bunit). The files next to this one are changes to merge into a project that already uses Verify snapshot testing with xUnit v3. Carry out the steps below in order.

## Steps

1. If the solution uses Central Package Management (a `Directory.Packages.props` with `ManagePackageVersionsCentrally`), merge the `PackageVersion` items from `Directory.Packages.props.fragment.xml` into it. Otherwise give each `PackageReference` in the next step the version listed in that file.
2. Merge `TestProject.csproj.fragment.xml` into the test project: the `PackageReference` items, and any properties and items it has.
3. Merge `ModuleInitializer.cs` into the test project's existing module initializer. Explicit `Initialize` calls go above `VerifierSettings.InitializePlugins()`, in the order given: a plugin can only be initialized once, so a call with parameters has to come before discovery. Calls after `InitializePlugins()` stay after it. If the project has no module initializer, add the file as it is.
4. Copy `Plugins/` into the test project.
5. The tests use placeholder types from `src-samples/`. Prefer pointing the tests at the project's real types; otherwise copy the placeholders into a project the tests reference.
6. Build, then run the tests with `DiffEngine_Disabled=true`. The new tests fail on their first run because they have no `.verified.` files yet. Show the user each `.received.` file; accept one only once the user agrees it is right.

## Verify plugins in this project

### Verify.Bunit

 * Snapshots a bUnit rendered Blazor component, with bUnit's full interaction api available first.
 * Repository: https://github.com/VerifyTests/Verify.Bunit
 * Enabled in `ModuleInitializer.cs` by `VerifyBunit.Initialize(excludeComponent: false);`. Do not move that call after `VerifierSettings.InitializePlugins()`.
 * Samples: `Plugins/BunitTests.cs`, using `Component`, `Nodes`.
 * Needs `using Bunit;` in the test file, not in global usings: plugins define colliding type names.
 * The samples use the bUnit v2 api, `BunitContext` and `context.Render<T>(...)`, not v1's `TestContext` and `RenderComponent`.
 * Verify.Bunit has no stable release on Verify 33 yet, so a prerelease version is referenced.
 * `VerifyBunit.Initialize()` registers bUnit's own markup comparer for html, which Verify.AngleSharp also does.
 * `component.WaitFor(...)` and `context.RenderComponentAndWait(...)` let a test verify a component that renders again after an async call.
 * The package suppresses BL0005 for the project that references it.

## Decisions already taken

These follow from the combination of plugins. Changing one of them changes behaviour across the whole test project.

 * **AngleSharp, Bunit**: Both register a string comparer for html. Verify.Bunit is initialized first, so Verify.AngleSharp's semantic comparison wins. In force: Verify.AngleSharp.
 * **AngleSharp**: VerifierSettings.InitializePlugins() cannot find AngleSharp: it looks for a type named after the assembly, and silently skips an assembly that has none. If the project relies on InitializePlugins() alone, they were never enabled, and each needs its own Initialize call in the module initializer.
 * **AngleSharp, Bunit**: Rendered html is a single long line, which makes snapshot diffs unreadable. `HtmlPrettyPrint.All()` formats every html snapshot, and Verify.AngleSharp compares them semantically rather than as text.

## Snapshot Testing (Verify)

This project uses [Verify](https://github.com/VerifyTests/Verify) for snapshot testing.

### Key concepts

- **`.verified.*` files** are the approved snapshots. They are committed to source control.
- **`.received.*` files** are the actual output from the latest test run. They are generated when a test fails (actual != expected) and are git-ignored.
- When a test fails, compare the `.received.*` file to the `.verified.*` file to understand the difference.

### Handling test failures

When a snapshot test fails:

1. Run the failing test and read the `.received.*` file that was generated.
2. Compare it to the corresponding `.verified.*` file.
3. Determine if the difference is expected (due to an intentional code change) or a bug.
   - **If expected**: copy the `.received.*` file to the `Verified:` path listed in the exception message to accept the new snapshot.
   - **If a bug**: fix the code, not the snapshot.
4. Never hand-edit `.verified.*` files to make tests pass. Always let Verify generate the correct output by running the test.
5. Never derive the verified name from the received name. The two are not always identical. Multi-targeted projects add a runtime and version suffix to the received file, and ignored parameters are kept in the received name but dropped from the verified name. Always take the destination from the `Verified:` line of the exception message.
6. Delete every `.verified.` file listed in the `Delete:` section. Those files are no longer produced by any test, usually because a test was renamed or removed.

If a snapshot differs between runs or between machines, add a scrubber to the test (`ScrubLinesContaining(...)`, `ScrubLines(...)`, `AddScrubber(...)`) rather than accepting the new output each time.

### Scrubbed values

Verify replaces non-deterministic values in snapshots with stable placeholders:

- GUIDs become `Guid_1`, `Guid_2`, etc.
- DateTimes become `DateTime_1`, `DateTime_2`, etc.
- File paths are replaced with tokens like `{SolutionDirectory}`, `{ProjectDirectory}`, `{TempPath}`.
- Custom scrubbers may replace other values.

These placeholders are intentional and ensure snapshots are deterministic across machines and runs.

### Exception message format

When a test fails, the exception message is machine-parsable with the following structure:

```
Directory: /path/to/test/project
New:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt
NotEqual:
  - Received: TestClass.Method.received.txt
    Verified: TestClass.Method.verified.txt

FileContent:

New:

Received: TestClass.Method.received.txt
<file content here>

NotEqual:

Received: TestClass.Method.received.txt
<received content>
Verified: TestClass.Method.verified.txt
<verified content>
```

 * The `Directory:` line gives the base path for all relative file references.
 * `New` means no `.verified.` file exists yet (first run or new test).
 * `NotEqual` means the `.received.` and `.verified.` files differ.
 * `Delete` means a `.verified.` file is no longer produced by any test.
 * The `FileContent:` section contains the actual text content for quick comparison without needing to read files separately.

See [Exception Message Format](https://github.com/VerifyTests/Verify/blob/main/docs/exception-message-format.md) for full details.

### Verified file conventions

- Encoding: UTF-8 with BOM
- Line endings: LF (not CRLF)
- No trailing newline

### Common patterns

```csharp
// Verify an object (serialized to JSON)
await Verify(myObject);

// Verify a string
await Verify("some text output");

// Verify with inline settings
await Verify(myObject)
    .ScrubLines(line => line.Contains("volatile"))
    .DontScrubDateTimes();

// Verify throws
await ThrowsTask(() => MethodThatThrows())
    .IgnoreStackTrace();
```

==== Directory.Packages.props.fragment.xml

<!-- Merge into the ItemGroup of PackageVersion items in Directory.Packages.props.
     Without Central Package Management, put each Version on the PackageReference in
     TestProject.csproj.fragment.xml instead, and skip this file. -->
<ItemGroup>
  <PackageVersion Include="Verify.Bunit" Version="1.0.0" />
  <PackageVersion Include="bunit" Version="1.0.0" />
  <PackageVersion Include="Microsoft.AspNetCore.Components" Version="1.0.0" />
</ItemGroup>

==== TestProject.csproj.fragment.xml

<!-- Merge into the test project. -->
<ItemGroup>
  <PackageReference Include="Verify.Bunit" />
  <PackageReference Include="bunit" />
  <PackageReference Include="Microsoft.AspNetCore.Components" />
</ItemGroup>

==== ModuleInitializer.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=AngleSharp&ext=Bunit
// Delete whatever is not needed.
using System.Runtime.CompilerServices;
using VerifyTests.AngleSharp;

// Everything here runs once, when the test assembly loads, before any test.
public static class ModuleInitializer
{
    [ModuleInitializer]
    public static void Initialize()
    {
        // Verify.Bunit: renders a component through bUnit and writes its markup as an html snapshot.
        // excludeComponent decides whether the component's own state is written alongside the markup.
        VerifyBunit.Initialize(excludeComponent: false);

        // Initializes every Verify.* plugin the project references and has not initialized above.
        // Explicit calls come first so their parameters apply: a plugin can only be initialized once.
        VerifierSettings.InitializePlugins();

        // Formats every html and htm snapshot before it is written, so a rendered page is a
        // readable tree rather than one long line. Takes an optional action for scrubbing the
        // nodes, for example _ => _.ScrubAttributes("id").
        HtmlPrettyPrint.All();
    }
}

==== Plugins/BunitTests.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=AngleSharp&ext=Bunit
// Delete whatever is not needed.
using Bunit;

// Verify.Bunit: Snapshots a bUnit rendered Blazor component, with bUnit's full interaction api available first.
// https://github.com/VerifyTests/Verify.Bunit
// Note: The samples use the bUnit v2 api, BunitContext and context.Render<T>(...), not v1's
// TestContext and RenderComponent.
// Note: Verify.Bunit has no stable release on Verify 33 yet, so a prerelease version is
// referenced.
// Note: VerifyBunit.Initialize() registers bUnit's own markup comparer for html, which
// Verify.AngleSharp also does.
// Note: component.WaitFor(...) and context.RenderComponentAndWait(...) let a test verify a
// component that renders again after an async call.
// Note: The package suppresses BL0005 for the project that references it.
public class BunitTests
{
    // The parameters are set through bUnit's builder, so they are checked against the component's
    // properties at compile time rather than matched by name at run time.
    [Fact]
    public Task Component()
    {
        using var context = new BunitContext();
        var component = context.Render<BunitComponent>(
            _ =>
            {
                _.Add(parameter => parameter.Title, "The Title");
                _.Add(parameter => parameter.Name, "Sam");
            });
        return Verify(component);
    }

    // Verifying the nodes, or a single node, snapshots only that markup: useful when a test is
    // about one part of a page and the rest changes for unrelated reasons.
    [Fact]
    public Task Nodes()
    {
        using var context = new BunitContext();
        var component = context.Render<BunitComponent>(
            _ => _.Add(parameter => parameter.Title, "The Title"));
        return Verify(component.Nodes);
    }
}

==== src-samples/BunitComponent.cs

// Generated by the Verify wizard: https://verifytests.github.io/Wizard/add?step=output&tf=XunitV3&have=AngleSharp&ext=Bunit
// Delete whatever is not needed.
using Microsoft.AspNetCore.Components;
using Microsoft.AspNetCore.Components.Rendering;

// Stands in for a real .razor component. BunitTests renders it and snapshots the markup.
public class BunitComponent :
    ComponentBase
{
    [Parameter]
    public string Title { get; set; } = "";

    [Parameter]
    public string Name { get; set; } = "";

    protected override void BuildRenderTree(RenderTreeBuilder builder)
    {
        builder.OpenElement(0, "div");
        builder.OpenElement(1, "h1");
        builder.AddContent(2, Title);
        builder.CloseElement();
        builder.OpenElement(3, "p");
        builder.AddContent(4, Name);
        builder.CloseElement();
        builder.CloseElement();
    }
}

